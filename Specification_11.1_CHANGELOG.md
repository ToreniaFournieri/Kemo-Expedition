## 11. CHANGELOG
**Update rule**
- Verion info is @Specification.md title part (ex: # KEMO EXPEDITION v0.6.3 - SPECIFICATION -> 0.6.3)
- Build info is @build_number.txt
- Entries must be sorted in descending order by Version, then Build.
- **descending order:** Overall list order must remain Latest → Older. So insert the new entry at the top in the table 
- Insert new entries at the top of the table.
- Date format: YYYY/MM/DD
- Language: English

| Version | Build | date | Changes                                                                              |
|-|-|-|-|
| 0.9.7 | 112 | 2026/09/23 | While an external `fundamental/logIn` session controls the game, the player may now control the UI except for commit operations (Spec 9.1.3 1-3). The header and tab content are no longer inert; the trusted UI adapter still refuses every commit with `apiControlActive`, and the UI controls that change game or runtime state without the adapter (sortie and Gods Battle buttons, Report Progress, auto-repeat, runtime mode, Orca level offset, debug settings, debug store purchase, party unlock, feedback reward) do nothing during the session. |
| 0.9.7 | 111 | 2026/09/23 | Fix API accounts seeing Instant Expedition charge that a sortie commit then refused as `charge_insufficient`. `read/observation/expedition` (charge, sortie controls, progress, side quest) and `read/expedition/{p}/chargeStock` now use the request's in-game clock (an API account's own clock; the player's real time) instead of the wall clock, matching `commit/expedition/{p}/sortie`. Add a regression for an account clock behind the wall clock. |
| 0.9.7 | 110 | 2026/09/23 | Allow main-view navigation while an external `fundamental/logIn` session controls the game. Header and tab content controls remain inert during the lease, while the trusted UI adapter refuses commits, including automatic view-mount acknowledgements; reads and external API commits continue. Add a focused session guard regression. |
| 0.9.7 | 109 | 2026/09/23 | Fix `read/expedition/{p}/latestBattleLog` returning `500 internal_error` after sorties with a reward. The generated public battle-room schema now accepts both stored reward end-event forms, `[2, item]` and `[2, item, autoSellGold]`; the previous schema required a third value and rejected ordinary drops. A fresh-sortie regression test validates the whole read response against the catalog and checks the two-field reward case. Regenerate the API contract. |
| 0.9.7 | 108 | 2026/09/23 | **`/api/v1` public cutover (Stage 9.7).** Every desktop build now offers the Application API v1 option in the Setting tab (`desktop/main.cjs` passes `allowEnable: true`); the test-only `--api-v1-test` launch flag is removed, including from `npm run test:api:desktop`. The API stays off on a fresh profile until the player enables it (with the Spec 8.6 confirmation), listens only on loopback, and authenticates with the §9.1.4.6 bootstrap token; the browser build shows no API option. Stage 9 gates met: conformance matrix and dual-adapter parity for all 85 operations, lifecycle suite, UI ownership audit, API option and token security checks, and no references to the retired `/experimental/v1` endpoint. A guard test keeps the gate from returning. |
| 0.9.7 | 107 | 2026/09/23 | Stage 9.6: the Spec 8.6 API option. `Application API v1`, `persistSecretToken` (default on), and a kept `secretToken` are stored by the desktop process in an owner-only `api-v1-settings.json` in the profile directory (never the save, a backup, or renderer storage). A remembered "on" restarts the listener at launch without a dialog; quitting keeps the option; disabling stops the listener and resets the token; with `persistSecretToken` the token is reused across launches, without it each launch creates a new one (turning it off deletes the stored token at once, turning it on stores the current one). The Setting tab's API panel confirms before enabling, adds the token-keeping switch, and shows the secret token hidden until the player clicks to reveal it (text in all five languages). New IPC (`desktop:set-api-v1-persist-secret-token`, `desktop:reveal-api-v1-secret-token`) and the enable IPC accept only the game window. `tests/apiV1Option.test.cjs` covers persistence, token rules, the token never appearing in settings, logs, URLs, or HTTP responses, the loopback-only listener, and a guard against references to the retired `/experimental/v1` endpoint. |
| 0.9.7 | 106 | 2026/09/23 | The Enemy Edit pane's open/closed state is now retained like the other Setting panes: `enemyEdit` joins the `setting.panelExpanded.<panel>` `uiPreferences` family (default open, as before) instead of resetting on every visit. |
| 0.9.7 | 105 | 2026/09/23 | Stage 9.3/9.4. Fixed: after a renderer crash or reload the desktop API server kept its control lease, so a client's new `fundamental/logIn` was refused with `control_unavailable` until the five-minute idle expiry; `desktop/api-v1.cjs` now exposes `releaseForRendererLoss()` (releases the lease and closes popup streams), called by `desktop/main.cjs` on `did-start-loading` and `render-process-gone` (Spec 9.1.4.16 updated). The server accepts a test-only `leaseIdleTimeoutMs` option (default five minutes). Added dual-adapter parity cells to the conformance matrix (every session operation compared between the HTTP transport and the trusted in-process adapter; closes the Stage 1 parity gate) and a lifecycle suite (`tests/apiV1Lifecycle.test.cjs`: account switching, simultaneous duplicates, receipt eviction, lease expiry and in-flight pinning, crash and renderer-loss recovery, shutdown draining). |
| 0.9.7 | 104 | 2026/09/23 | Stage 9.5d/9.5e. The Setting tab no longer receives the complete `GameState`: News unread state comes from a new `isRead` on each `resources/developerNewsNotification` entry; Clairvoyance panes come from `resources/clairvoyance/{p}`; the Character Roster reads every party's members from `read/observation/party`'s `parties` list (now with `gender` and `isUnique`); Send Feedback's save-derived report lines and attachments are built by `HomeScreen` (`onBuildFeedbackReport`, removing a duplicated battle-log HTML builder). Fixed an API rule gap: `resources/clairvoyance/{p}` now returns `{available: false}` when no member has `a.prophecy` and the Debug override is off (Spec 9.1.3 4-2-3), otherwise `available`, `canReset`, and the facts; `commit/setting/clairvoyanceReset` now requires `a.prophecy`2 or the Debug override (`illegal_action`). Both use the new shared `src/game/clairvoyanceAccess.ts`. Guards reject the `gameState` prop and any Setting-tab raw save read except the reviewed party count and language; the conformance matrix covers the reset refusal. |
| 0.9.7 | 103 | 2026/09/23 | Stage 9.5 audit fixes. (A) The Setting tab's retained pane expansion and per-party Clairvoyance expansion (Spec 8.6) move from local storage to per-save `uiPreferences` (`setting.panelExpanded.<panel>`, `setting.clairvoyanceExpanded.<partyNumber>`) as Spec 9.1.4.17 requires; (B) the Glossary tab is retained as `setting.glossaryTab` (the default `能` applies only until a tab is stored), and expanded Glossary entries become local view state. The preference catalog gains `settingPanel`, `partyNumber`, and `none` subjects with a published `subjectOptions` list. `HomeScreen` migrates the old local values once (a stored preference wins) and removes the old keys. (C) For the ordinary player, `read/setting/debug` and `settingInfo.debug` report the Debug pane's real settings and `commit/setting/debug` applies changes to them after the durable commit (new `src/api/v1/debugSettings.ts`, runtime ports `debugSettings`/`applyDebugSettings`); an API account keeps its own stored debug settings. Added the Stage 9.2 conformance matrix (`tests/apiV1ConformanceMatrix.test.cjs`) and Stage 9.5 ownership guards; `Specification_9.1.4_API_DETAIL.md` documents the new preference families and the debug rule. |
| 0.9.7 | 102 | 2026/09/23 | Stage 9.1a/9.1b: Mode Select display settings are real. `commit/setting/modeSelect` previously stored `theme`, `darkMode`, `showExpeditionStats`, and `autoRepeat` in control settings and `read/setting/modeSelect`/`settingInfo.modeSelect` echoed them back without changing the game. A new shared module (`src/api/v1/modeSelect.ts`) and runtime ports (`displaySettings`, `applyDisplaySettings`) now read the runtime's real values and apply validated changes after the durable commit (the sortie precedent); a display-only change does not change the save or revision. Themes are validated against the Spec 8.6 environment rules (`validOptions.theme` lists only selectable themes; `mode.orca` and beta lock it). `autoRepeat` is read-only per Spec 9.1.3 3-6-2 (`invalid_request` if supplied). An API account reads all four as `null` and a change is `illegal_action`. The Setting tab's theme, dark mode, and statistics controls now commit through `modeSelect`; the stale echoed copy is dropped from control settings. Contract regenerated; `Specification_9.1.4_API_DETAIL.md` 9.1.4.14 documents the rules; tests cover the commit rules, the in-process flow, the API-account case, and the Setting-tab wiring guard. |
| 0.9.7 | 101 | 2026/09/23 | Stage 9.0 API cleanup. A rejected character edit now shows a localized notification with the error code (`home.notification.characterEditFailed`, all five languages) instead of failing silently to the console; the Party-tab migration guard lists this toast as a reviewed local action. Removed the unused `getExperimentalDiaryTitle` helper from `homeShared.tsx`. Repointed four `SpecRef: 9.1.3 \| Experimental AI API` comments (`shop.ts`, `savePersistence.ts`, `battleSeedSource.ts`, `gameplayRandom.ts`) to real sections (9.1.3 2-4-3 `shopInfo`; 9.1.4.4). Dropped stale "(migration in progress)" test names in `tests/migratedTabs.test.cjs`. |
| 0.9.7 | 100 | 2026/09/22 | Complete Stage 8 by migrating the five deferred read-only Settings reference panels to the trusted in-process Application API. `HomeScreen` now reads `resources/glossary`, `resources/itemCompendium`, `resources/characterRoster`, `resources/bestiary`, and `resources/superRareList` while Settings is visible and passes their projected facts into `SettingTab`; the panels use those resources for glossary disclosure/content, item disclosure/catalog membership, roster membership/base status, Bestiary disclosure/encounter/defeat counts, and the Super Rare catalog. Presentation-only grouping, images, localized formatting, filters, expansion state, and explicit Debug reveal overrides remain local. Remove the panels' direct reads of glossary/item reveal arrays and global enemy battle statistics, and extend the mechanical migration guard to require every resource operation and projected prop. Send Feedback remains an intentionally reviewed local exception outside Stage 8's remaining scope by user decision. |
| 0.9.7 | 99 | 2026/09/22 | Stage 8: Settings UI migration, the small/high-value panels (Backup/Reset, News, Donation, Clairvoyance, Enemy Edit's dropdown option keys, and Mode Select's `language` field), per a user decision to defer the five large read-only reference panels (Glossary, Item Compendium, Character Roster, Bestiary, Super Rare) and to keep Send Feedback a reviewed exception alongside Report Progress. Two correctness findings surfaced while implementing this, both fixed before wiring the UI: (1) `resources/developerNewsNotification` returned each entry's whole `Record<Language, string>` content object instead of the current language's string, violating its own published response schema (`content: Type.String()`) — fixed to index by `state.global.language`, per Spec 9.1.3 §4-2-1. (2) Routing Backup/Reset through the generic API commit path would have silently swapped the existing `replaceDurable` full-replacement persistence (which correctly garbage-collects the prior Diary-record generation on import/reset) for the ordinary coalescing autosave (`persistApiState`/`requestOrdinary`, which assumes incremental continuity and does not) — a real save-corruption risk for the ordinary in-app player. Fixed by adding a second persist port: `SerializedApplicationApiAuthority`'s commit path now calls a new `ports.runtime.persistPlayerReplacement` (wired to a new `actions.persistApiStateReplacement` → `coordinator.replaceDurable`) specifically for `commit/setting/backup/import`/`reset`, leaving every other commit on the ordinary path unchanged; guarded by a new test in `tests/persistenceConsistency.test.ts`. Export/Import/Reset keep their existing envelope, platform-sharing code (iOS share sheet / Chromium file picker / download fallback), and separate `runtimeSnapshot` (live party-cycle/AFK state, still explicitly outside the API's domain per Stage 4) exactly as they are — only the `GameState` bytes now flow through `commit/setting/backup/export`/`import`/`reset` via a new `InProcessApiAdapter.commit` `uploadedFiles` parameter, with `skipConfirmation: true` since the UI already runs its own native confirm dialogs. News/Donation/Clairvoyance move to `resources/developerNewsNotification`/`resources/donationBox`/`resources/clairvoyance/{p}` reads and `commit/setting/markNewsAsRead`/`commit/setting/clairvoyanceReset` commits, replacing raw `GameState` reads and five separate reducer actions; Enemy Edit's enemy-type dropdown reads its option keys from `read/setting/enemyEditPane`'s `validOptions` (closing a real drift risk in the file's own hand-maintained label map) while terrain effect stays locally derived (already sourced from the same canonical glossary data, so switching added no value) and the edited value stays local by design (an ordinary player's `control.settings` is never durably persisted, unlike `GameState` itself — same reviewed-exception treatment as Debug). Add a new, narrower migration guard to `tests/migratedTabs.test.cjs` (the Setting tab remains intentionally partially migrated — Glossary/Compendium/Roster/Bestiary/Super Rare/Debug/Feedback are out of scope and still use raw state/reducer actions). Verified interactively in a running browser build: News render and mark-as-read, Donation ranks, Clairvoyance's full per-bag/per-tier breakdown, a full Export, a full Reset (durably persisted across a reload), and a language switch to English (including the `ensureLanguageLoaded` pre-load this needed, since `setLanguage` throws on an unloaded dictionary) — the whole UI re-rendered correctly with no new console errors. Stage 8's only remaining item is the five deferred reference panels. |
| 0.9.7 | 98 | 2026/09/22 | Stage 8: build the delivery sender, and correct this document's stale claim that Help was unbuilt (it already returns the live `Specification_9.1.3_API.md`/`Specification_9.1.4_API_DETAIL.md` text via `HomeScreen.tsx`'s Vite `?raw` imports and has since before Stage 8 began — no code change needed there, only the doc). `commit/progress/progressReport` and `commit/setting/feedback` now actually deliver instead of sitting at `queued` forever: content and attachment bytes are captured once at commit time (`src/api/v1/deliveryContent.ts`, reusing `computePartyStats`/`computeCharacterStats`, `backup/export`'s own `encodePersistedState(serializeGameState(...))`, and `battleLogs.ts`'s `buildBattleLogData` — not the UI buttons' HTML-table code, which is presentation-layer and was already duplicated between `HomeScreen.tsx` and `SettingTab.tsx`, not a clean shared source to reuse). A new `SerializedApplicationApiAuthority.runInternalTransaction` (`authority.ts`) gives the sender's claim/settle/complete steps the same persist/publish/revision discipline as a client commit, serialized against it, with no idempotency key, confirmation, or receipt (it is never a client request). `ApplicationApi.pumpDeliveries`/`startDeliveryPump` (`applicationApi.ts`) run claim → send → settle → complete, firing immediately after a commit that queues a job and on a 5-second backstop interval; the actual network POST is an injected port (`ApplicationApiPorts.delivery.send`), implemented in `HomeScreen.tsx` by generalizing the existing `postWebhookWithFiles` Discord-webhook pattern — the two existing UI buttons (Report Progress, Send Feedback) are untouched, still separate reviewed exceptions with their own cooldowns. Per 9.1.4.15: a settle-persist failure retries only the persist, never the send; `not_sent` retries automatically up to the existing 3-attempt cap; `rejected`/`ambiguous` settle immediately to `failed`/`unknown` and are never auto-resent; the frozen payload is dropped once a job can never send again, keeping persisted control metadata bounded. Add `settingInfo.pendingDeliveryIds` (queued/sending/unknown) per 9.1.4.15's "the setting/overview projection includes relevant pending IDs." Add `tests/apiV1DeliverySender.test.cjs` covering content capture, the full claim/send/settle/complete cycle for both operations (including the feedback reward grant), `not_sent` retry-then-fail, `rejected`/`ambiguous` terminal handling, the settle-persist-retry case (send is never called twice), and `pendingDeliveryIds`. Stage 8's only remaining item is the Settings UI migration. |
| 0.9.7 | 97 | 2026/09/22 | Implement the new `skipConfirmation` parameter added to Specification 9.1.3, 3-6-5-2 (`backup/import`) and 3-6-5-3 (`backup/reset`). An optional boolean (default `false`) inside `parameters`; when `true`, `resolveConfirmationPolicy` (`src/api/v1/confirmationPolicy.ts`) returns no policy for that request, so `executeApiV1CommitTransaction` never issues a `confirmation_required` challenge and commits in one round trip, exactly as if a valid token were already held — the operation's own mutation, idempotency, revision, and `delivery_in_flight` fencing rules are unchanged, and no other operation is affected. Regenerate the request-schema catalog (`scripts/generate-api-v1-contract.mjs`, `desktop/api-v1-contract.json`) so both operations' `parameters` accept `skipConfirmation`. Update `Specification_9.1.4_API_DETAIL.md` §9.1.4.5 and §9.1.4.10 (AI-owned per AGENTS.md) to document the bypass and correct the multipart import parameters description, which had said `parameters: {}`. Add authority-level coverage (bypass commits immediately for both operations; omitted/`false`/non-boolean still challenges; a delivery in flight still refuses the commit even with the bypass) and HTTP-transport coverage (the wire schema accepts `skipConfirmation` over both JSON and multipart transport and still rejects any other unlisted parameter) to `tests/apiV1Backup.test.cjs`/`tests/apiV1BackupTransport.test.cjs`. |
| 0.9.7 | 96 | 2026/09/22 | Begin Stage 8's remaining work with backup/import/reset test coverage. Writing a real (non-ASCII-safe) round-trip test for `commit/setting/backup/import` surfaced a genuine decode bug, not just a coverage gap: `commitOperations.ts` decoded the uploaded backup with plain `atob(contentBase64)`, but the uploaded bytes are the UTF-8 encoding of the compressed save string (`desktop/api-v1.cjs`'s raw-binary export writes `Buffer.from(savePayload, 'utf8')`), and that string routinely contains code points above Latin1 range (LZ-string's UTF16 packing puts real payloads well past 255). `atob` alone cannot reverse a UTF-8 byte encoding, so importing a real save's exported backup would throw `Failed to decode compressed save payload` almost every time; fixed by pairing `atob` with a `TextDecoder` UTF-8 decode of the recovered bytes, verified to round-trip Japanese text and emoji correctly. Add `tests/apiV1Backup.test.cjs` (commit-level export/import/reset behavior including the corrected non-ASCII round-trip and `invalid_backup` refusals; authority-level revision high-water preservation across a confirmed reset/import instead of resetting to 0/1, popup-event/confirmation clearing, queued-delivery cancellation, and the `delivery_in_flight` refusal with its stable reason for both operations; and a transport-neutral `ApplicationApi.handle` round trip through the real confirmation-required/confirmed cycle) and `tests/apiV1BackupTransport.test.cjs` (the real desktop HTTP server: the raw-binary export response's headers and exact byte content, real `multipart/form-data` parsing delivering the exact uploaded bytes to the authority, malformed-multipart refusals, and that a confirmed import closes every open popup stream synchronously like reset already did). Stage 8's remaining work is now Help document-serving, the delivery sender, and Settings UI migration. |
| 0.9.7 | 95 | 2026/09/22 | Close the last 2 of the 6 `Type.Unknown` Resource gaps from Stage 1's contract-catalog gate, completing it. `resources/characterRoster` now filters to the requested `race` (fixing a missing `mustelid` from the query's own enum, which made it unreachable), returns real base status (`race.stats`, unchanged), reuses `describeBonuses` for the same stable ability/cBonus/otherBonus vocabulary `itemCompendium`/`bestiary` already use (excluding ability-type bonuses, which are redundant with the separate `defaultAbility`/`unlockAbility` fields), and returns those two fields as the same stable ability IDs already stored on `Race.defaultAbility.id`/`unlockAbility.id` instead of the raw definition objects. `resources/clairvoyance/{p}` now reports real remaining/total and hits-remaining/hits-total counts for every reward, enhancement (with its 6 named tiers), super-rare, and side-quest bag, plus the three-way sleepiness breakdown, by comparing each party's live `RandomBag` against a freshly created bag of the same kind (`src/game/bags.ts`'s existing `createXBag`/`getBagTicketTotal`/`getBagEntryTickets`) rather than storing a duplicate "total" anywhere. Add schema and real-save coverage for both. Stage 1's contract catalog is now fully closed; Stage 8's remaining work is backup/import/reset test coverage, Help document-serving, the delivery sender, and Settings UI migration. |
| 0.9.7 | 94 | 2026/09/22 | Begin Stage 8 by closing 4 of the 5 `Type.Unknown` Resource gaps from Stage 1's contract-catalog gate. `resources/itemCompendium` now returns every item at base level (Enhancement/SuperRare = 0) through the existing `describeItem` bonus vocabulary, honors its `category`/`rarity`/`itemId` filters, and adds a `revealed` flag from `revealedItemCompendiumItemIds` (items are never omitted, only flagged, per Spec 8.6's "shows all items, including undiscovered"). `resources/bestiary` reuses `buildEnemyStatus` (already written for `latestBattleLog`'s bottleneck enemies) and adds shared, save-wide `encounters`/`defeats` counts (`GlobalState.enemyBattleStats`) and a `revealed` flag (encountered at least once), plus `enemyId`/`enemyType`/`expedition` filters. `read/setting/enemyEditPane`'s `terrainEffect`/`enemyType` valid-option lists now come from the real terrain glossary and enemy master data instead of `['none']`/`[]`. `resources/glossary` now projects the real, localized `GLOSSARY_SECTIONS` content and replaces its previously-invented `category` enum (`Ab.`, `Base.`, ...) with the letter prefixes `Specification_1.1_CONSTANTS_GLOSSARY.md`'s own section numbering already uses everywhere else (`a.`, `b.`, `c.`, `d.`, `f.`, `g.`, `m.`, `q.`, `t.`); only `a.` (ability) and `t.` (terrain) entries are reveal-gated per rule 1.0.3, the other 7 categories are always visible. `resources/characterRoster` and `resources/clairvoyance/{p}` are deliberately deferred: the roster needs the Party-status-pane's combined bonus-line formatter (a different code path than the ability/cBonus/otherBonus split used here), and clairvoyance needs a larger remaining/total-per-bag shape across roughly 15 named bags. Add schema and real-save coverage for all four fixes. |
| 0.9.7 | 93 | 2026/09/22 | Fix a Build 92 regression that froze the whole app (no button responded, no progress advanced) on both desktop and web. The new `read/observation/overview` read added for the header migration depended on `headerRuntimeRef.current`, a plain object HomeScreen reassigns to a brand-new reference on every render; React's dependency comparison therefore saw a "changed" dependency on every render, re-triggering the projection read every render, whose resolution triggered another render, in an infinite loop that starved the event loop. Depend on the underlying primitive values (`effectiveDebugSettings.timeSpeed`, `timeSpeedBonusUntilMs`, `isAutoRepeatEnabled`) instead of the ref object, matching how every other `useApiRead` call in `HomeScreen.tsx` already lists primitive/stable dependencies. Verified interactively: tab switching, a Shop purchase, and live gold/progress updates all work again. |
| 0.9.7 | 92 | 2026/09/22 | Finish Stage 4 by migrating the header to the Application API. A new `src/components/home/HeaderBar.tsx` renders the fixed top bar (title, orca suffix, version label, Speed-of-Time symbol/countdown, gold, auto-repeat button) from `read/observation/overview`'s `headerInfo` projection through a new pure view builder (`src/api/v1/headerView.ts`), instead of raw `state.global.gold` and local debug-settings refs; the Speed-of-Time label/symbol logic, including the orca-mode `x5`→`x6` bonus case, is ported unchanged, now keyed off the projected `speedOfTime`/`gameMode` facts. Two controls remain reviewed local exceptions, each recorded with its reason: Report Progress still performs its own webhook call directly (the matching `commit/progress/progressReport` has no real network sender until Stage 8), and auto-repeat stays local/runtime state (it is one field of the spec's bundled `commit/setting/modeSelect`, explicit Stage 8 scope, not a header-only concern). Both reviewed reducer/webhook calls live in a named `handleReportProgress` handler outside the guarded JSX. Add the mechanical migration guard for the header, mirroring the existing Party/Diary tests. Stage 4 is complete; Stage 8 (Settings, files, delivery sender, Help, Resources) is next. |
| 0.9.7 | 91 | 2026/09/22 | Complete Stage 7 with slice D4, the SSE lifecycle, on top of Build 90's durable popup buffer. The transaction authority now fires a transport-neutral `notifyPopupActivity` signal after every durably committed, changed commit (never on a no-op or an idempotent receipt replay); the desktop renderer relays it to the main process over a new one-way IPC channel, and the HTTP layer pushes every open `popupEventStream` connection immediately instead of waiting for its 1-second poll, which remains as a resilience backstop and now renews the control lease only when it actually delivers an event. A cursor-not-found read now returns the catalogued `invalid_cursor` (400) instead of an uncatalogued code, and an absent `Last-Event-ID` now consistently anchors the replay boundary at connect time (fixing a case where the very first event pushed after a fresh connect with an already-populated buffer could be silently dropped). `commit/setting/backup/import` and `commit/setting/backup/reset` now close every open stream with `resyncRequired` synchronously in the same request, instead of waiting for the next poll tick to discover the fenced buffer. Add HTTP-level lifecycle coverage (push, reconnect replay, invalid-cursor rejection, mid-stream resync, synchronous reset/logout/shutdown closing) and authority-level coverage of the new notify signal. Stage 7 is complete; the Stage 4 header UI migration is next. |
| 0.9.7 | 90 | 2026/09/22 | Complete Stage 7 slice D3 with durable popup production. The Application API now derives all four configured popup categories as language-neutral semantic events from each transaction's immutable before/after snapshots: Cycle events (including Instant Expedition, level, and Party unlock facts), Item Format drop events with rarity and the existing 20-item suppression, exact automatic-equipment slot changes, and Side Quest acceptance/completion/failure/Gods Battle cancellation. Controlled elapsed progression and login catch-up preserve the existing AFK rule by producing at most one grouped Cycle summary per affected Party instead of per-event bursts. The authority assigns deterministic `revision:sequence` IDs, persists the typed v1/schema-1 buffer atomically with state, retained references, receipt, clock, RNG, and revision, retains the union of the latest 256 events and five minutes, normalizes pre-D3 records, clears the buffer with import/reset, emits nothing on failed persistence, and creates nothing on receipt replay. Update the generated desktop contract and add focused category/settings, AFK, retention, transaction, reset-fence, replay, and login-catch-up coverage. Stage 7 D4 SSE delivery and lifecycle handling is next. |
| 0.9.7 | 89 | 2026/09/22 | Complete Stage 7 slice D2 by migrating the Diary pane to the Application API. `DiaryTab` now receives a typed view built only from `read/observation/diary` summaries and each entry's `diary:<id>` `latestBattleLog` response; it no longer receives saved Party/Diary objects, re-renders compact narration from API resources, and preserves unchanged legacy-only labels. The Diary projection now includes each Party's current character display identity so retained compact battles use the latest character name while their recorded appearance remains historical. Settings, opening an unread entry, leaving the pane, switching Party, and desktop-notification acknowledgement now use Diary commits; the persisted shared Party selection is the one reviewed reducer exception, replacing the Diary-only undocumented local-storage selection. Add the renderer adapter, focused mixed-history assertions, generated-schema coverage, and a mechanical ownership guard. Stage 7 D3 durable popup production is next. |
| 0.9.7 | 88 | 2026/09/22 | Begin Stage 7 with the complete Diary contract and command foundation. Preserve the save's opaque string Diary IDs in the v1 technical contract, replace the raw `metadata: Unknown` projection with a closed language-neutral summary (effective selection, current Party name, unread counts, exact settings, ordered triggers, dungeon/difficulty, Item Format rewards, side-quest/unlock facts, semantic or unchanged legacy content, and `diary:<id>` battle-log references), and make focused entry reads return the same real content instead of placeholders. `read/diary/{p}/diarySetting` now publishes all setting options. `markAsRead` now honors the optional Party scope, scopes `ALL`, rejects unknown/mismatched/duplicate IDs atomically, and returns the remaining total. Add generated-schema coverage, mixed compact/legacy and locale-independence tests, in-process versus HTTP-shaped parity, and validation of every retained Diary entry in the real six-party save. The Diary UI and complete popup/SSE behavior remain the next Stage 7 slices. |
| 0.9.7 | 87 | 2026/09/22 | Stage 6 (Base), slice B4c (final): migrate the Inventory pane to the Application API. The pane now draws the Base projection's inventory facts (`src/api/v1/inventoryView.ts` rebuilds items and worn-item owners from their Item Format and master data): every variant with its sale value, held Jewels, and every worn item with its owner, so the pane holds no inventory game state and imports no sell-price or Prana rule. Selling, unlocking a sold variant, and the Jewel Priority Party commit through `sellInventoryItems`, `unlockSoldItems`, and `changeJewelPriorityParty`; the highlight on a newly acquired variant is acknowledged through `markItemsAsSeen` for exactly the variants the pane displays as new, replacing the previous blanket `markItemsSeen` action. Remove the now-dead `sellStack`, `sellAllOwned`, `setVariantStatus`, `markItemsSeen`, and `setJewelAutoEquipPriorityParty` reducer-hook actions. Add the typed inventory read models and a migration guard. Verified in a running browser build against a real save: selling a stack moved Gold by the displayed price and removed the stack; the sold item appeared in the auto-sell list and "解除" (unlock) removed it; the Jewel Priority Party selector listed and set every unlocked party; Jewel entries showed their equipped-item owners. This completes Stage 6 (Base); Stage 7 (Diary and popup streaming) is next. |
| 0.9.7 | 86 | 2026/09/22 | Stage 6 (Base), slice B4b: migrate the Altar pane to the Application API. The pane draws the Base projection's `altar` (the Prana balance and every enemy category's Alter level, victories, and next-level requirement) and a single `read/base/enemyFormList` read for all categories (so switching category is instant and never blank), and each form's cost, required and current Alter level, whether it is unlocked, and whether it can be unlocked now are the API's facts; unlocking commits through `unlockForm`. The pane no longer receives the victories or unlocked-form state and no longer imports the Alter-level, cost, or unlock rules (a form's name, abilities, and bonuses stay master data for the projected enemy ID). Add the typed Altar read models to `src/api/v1/baseView.ts`, remove the dead `unlockMimorianEnemy` reducer-hook action, and add a migration guard. Verified in a running browser build against a real save: the projected Prana, the category's level and victories (level 0, 116 of 150), and the forms with their costs and required levels render, with unlocked forms marked. |
| 0.9.7 | 85 | 2026/09/22 | Stage 6 (Base), slice B4a: migrate the Shop pane to the Application API. The pane now draws the Base projection's `shop` (dialogue tier, countdown, refresh price and availability, and each slot's price, rarity, sold-out, and availability; typed read model in `src/api/v1/baseView.ts`), and buying and the paid refresh commit through `purchaseShopItems` and `paidShopRefresh` in order. `HomeScreen` reads the Base projection only while the Base tab is visible, keeps the last result while hidden, and re-reads when the next scheduled shop refresh arrives; the pane no longer receives the shop's purchases, refresh counts, or intimacy and no longer imports the shop rules (the purchase notifications remain driven by the published state). Remove the now-dead `buyShopItem` and `refreshShopLineup` reducer-hook actions. Add a migration guard, and verify in a running browser build that the pane renders the projected lineup, that buying commits (Gold 200 to 140, the slot shows sold out, the purchase toast appears), and that the paid refresh is disabled when the Gold is short. |
| 0.9.7 | 84 | 2026/09/22 | Stage 6 (Base), slice B3: the Inventory reads. The `base` projection now carries what the Inventory pane needs: every inventory variant with its `sale` (`{gold, prana}` for an owned stack), computed by a new shared `getStackSale` that the sale itself now uses, so the advertised amount is exactly what selling does (a Super Rare item pays Prana only, never Gold); the unassigned Jewels held (`jewels`); and every worn item with its owner, member position, slot, attached Jewel, and whether the slot is within the character's slot count (`equippedItems`). `commit/base/markItemsAsSeen` acknowledges only the given variants (unknown is `not_found` for the whole request, empty or duplicate is `invalid_request`) and returns only the variants that actually changed, where it had returned every requested key; `commit/base/changeJewelPriorityParty` refuses a party that does not exist with `not_found` instead of quietly falling back to PT1. The Ashen Route Vault has no API operation (Spec 8.4.3) and is unchanged. Add tests for the sale equals the effect, jewels, worn items and their owners, the acknowledgement rules, and the priority validation, plus a schema and consistency check of the projection on the real six-party save (every variant, worn item, attached Jewel, and held Jewel). |
| 0.9.7 | 83 | 2026/09/22 | Stage 6 (Base), slice B2: the Altar and enemy forms. Add `src/game/altarFacts.ts`, the one source of the per-category Alter level, victories, and next-level requirement, and of each enemy form's Prana cost, required Alter level, and the reason it cannot be unlocked. `read/base/altarInfo` and the `base` projection's new `altar` section publish the Prana balance, the maximum level, the unlocked forms, and every enemy category's level, victories, and form counts (previously raw donation and victory dumps). `read/base/enemyFormList` returns real entries instead of placeholders (cost 0, no bonuses, no condition): canonical name and tier, the abilities and bonuses a Mimorian would copy, the cost, a structured `unlockCondition`, `unlocked`, and `unlockable` with a stable reason, with optional `enemyType` and `enemyId` filters; its `validOptions.enemyId` lists exactly the forms `unlockForm` accepts. `commit/base/unlockForm` now refuses `already_unlocked`, `altar_level_too_low`, and `insufficient_prana` with `illegal_action` (the reducer ignored them silently), `not_found` for an unknown enemy, and returns `{enemyId, pranaDelta}`. Add tests for every reason, per-category victories, the filter, and that every offered form is accepted and every refused form is refused for its stated reason. |
| 0.9.7 | 82 | 2026/09/22 | Close the E3 narration gap per the updated Spec 9.1.3, 2-2-2. `read/expedition/{p}/latestBattleLog` now returns `resources` beside `battleLog`: for every room the stored language-neutral records (the compact battle envelope, the end events with their flavor facts, the gate text, the reward items, the post-battle HP, and the Gods Battle flag), the enemy as it was met for the Bestiary bubble (only its documented members, so an older snapshot's obsolete fields are not published), and, for a legacy record only, the prose it was saved with; the Expedition projection's `exploration.resources` does the same for the revealed rooms. The pane's log view is now rebuilt from API responses alone, with no game-state input: the retained log is no longer passed to the adapter, the approximate fallback narration and Build 79's room-matching guard are removed, and `renderExpeditionMetadata` and `renderDiaryBattle` render names, gate and reward text, and the full narration in the current language. Add concrete schemas for the resources (compact envelope, end events, enemy snapshot, legacy entries, recursive gate text). Rendering from the response is verified identical to rendering the retained record for all 1,974 rooms of the real six-party save and for a freshly resolved compact expedition (mutation-checked for the compact battle and the end events). Fix the blank pane on tab switch: `useApiReadMany` now keeps its last result while disabled (as `useApiRead` already did), so a hidden Expedition tab shows its previous content immediately instead of an empty pane until the re-read finishes. Also remove an unused import left in the Build 81 test. |
| 0.9.7 | 81 | 2026/09/22 | Stage 6 (Base), slice B1: the Shop. Add `src/game/shopFacts.ts`, the one source of the shop's dialogue tier, effective intimacy, refresh countdown and price, and slot IDs. `read/base/shopInfo`, `shopItemsList`, and the `base` projection now publish real facts instead of placeholders: the dialogue tier key, the countdown to the next refresh in seconds, the effective (decayed) intimacy, and the paid-refresh price by refresh count (the projection had used the intimacy value as the refresh count). `shopItemsList` returns the compact strings of Spec 9.1.3 in `current.items` and `validOptions.items`, with the same facts structured in `current.entries` (`soldOut`, `unavailableReason`, `rarity`); shop slot IDs are now the integer 1-based lineup positions instead of internal string keys. `purchaseShopItems` validates every entry against one snapshot (malformed or duplicate is `invalid_request`, an unknown slot `not_found`, sold out or insufficient Gold `illegal_action`) and `paidShopRefresh` refuses an unaffordable refresh (the reducer had ignored it silently) and returns `{lineupId, goldDelta, paidRefreshPrice}`. The purchase and refresh reducers gained an optional `now`, and the API passes its transaction time: previously they used the wall clock, so an API account whose in-game clock differed from real time validated one lineup and bought from another. Add commit and read tests, including the account-clock case, and regenerate the contract. |
| 0.9.7 | 80 | 2026/09/22 | Fix the ordinary player's Application API clock, found while starting Stage 6 (Base). The trusted in-process adapter used one fixed timestamp for the player's whole session (the time the app started, refreshed only at API logout) as both `inGameTime` and the transaction time, while the UI uses the real clock everywhere. Since Build 70/74 the Sortie and Gods Battle buttons therefore committed with a stale time: the Instant Expedition charge was computed at the app's start time, so charges that had recharged since then were refused (`charge_insufficient`, logged only to the console), and the consumed charge timer and the recorded expedition time were wrong. The player's transactions and reads now use the wall clock of each request (`playerClock`, monotonic); an API account keeps its own in-game clock, which only its progression advances. Add a regression that spends a charge that recharged after three hours and reads the current time (mutation-checked: it fails without the fix). |
| 0.9.7 | 79 | 2026/09/22 | Fix two Expedition-tab E3 defects found in review. (1) While a party explored, the pane showed the previous expedition's rooms: the tab sliced the disclosed log (`latestBattleLog`) by the running exploration's revealed count, so with the real six-party sample save it listed dungeon 7's enemies for a dungeon 8 run. The Expedition projection's `exploration` now carries `dungeonId`, `difficultyOffset`, `totalRooms`, and the revealed rooms in full (the public `latestBattleLog` room shape, shared through `buildBattleRoomData`), the pane builds its view from those (`buildPartyExpeditionLogView`, with no result, experience, or rewards until the exploration ends), and future rooms are still never sent. (2) Narration was copied from the retained log by room number alone, so a retained log newer or older than the projection (they are read at different moments) could lend its narration to another room; it is now used only for a room that matches on room, floor, enemy, outcome, damage dealt and taken, and remaining HP, and only for the same dungeon, otherwise the public-fact fallback narration is used. Regenerate the contract, correct the test that had asserted the old behavior, and add a regression that drives an exploring party through the view path (mutation-checked: it fails against the old behavior) plus mismatch cases for every matched fact. |
| 0.9.7 | 78 | 2026/09/21 | Complete Expedition-tab slice E3: the quick summary, room list, rewards, HP facts, and battle-log source now come from each party's `read/expedition/{p}/latestBattleLog` projection through the trusted in-process adapter. Add `src/api/v1/expeditionLogView.ts` to rebuild the renderer view from language-neutral API facts, provide localized fallback narration for compact/legacy wire events, and confine retained-log narration details to the adapter boundary; the tab no longer reads `party.lastExpeditionLog` directly. Add projection-authority, fallback-narration, stale-retained-fact, and migration-guard tests. |
| 0.9.7 | 77 | 2026/09/21 | Complete Expedition-tab slice E2: the pane now reads party state, progress clocks and step boundaries, revealed-room count, HP, Instant Expedition charge, disclosed floor/outcome, Clear-Gates, side quest, and Sortie/Gods Battle availability from `read/observation/expedition` through the trusted in-process adapter. Continuous bars and countdowns interpolate only from projected clocks; step-based facts re-read at `nextChangeAt`/`nextRevealAt`, so the renderer no longer derives future exploration rooms from an already-resolved log. Remove the `partyCycles` prop and local no-spoiler/availability calculations from the tab, add a typed Expedition projection view and projected goal/side-quest formatting, and add a migration guard. Raw retained-log narration remains explicitly scoped to E3. |
| 0.9.7 | 76 | 2026/09/21 | Expedition tab slice E4: the forecast. The Expedition pane's Simulation Run button now reads the Application API's private 1,000-run `read/expedition/{p}/simulationRun` through the trusted in-process adapter instead of calling the game engine directly, and rebuilds the graph, tooltips, and summary from the projection with a new exact inverse (`parseSimulationRunData`). To make that lossless, the projection gains exact terminal `counts` (Clear and Return split, no rounding). The read returns when all runs finish, so the running `completed/total` count is replaced by a plain "Simulating" label in all five languages (the pane no longer passes a progress callback). Remove the now-dead `simulateExpedition` reducer-hook action. Add a round-trip test with a distinct value in every counter and migration guards that the tab has no progress callback and the forecast goes through the API. |
| 0.9.7 | 75 | 2026/09/21 | Implement `commit/expedition/{p}/resetStatistics` (Spec 9.1.3, 3-2-4): the Expedition pane's Reset button as an Application API command that restores the party's expedition statistics to their defaults with the same reducer action and changes nothing else (no parameters, returns `{}`, a no-op when already default, `not_found` for an unknown party). The operation count is now 85: add it to the 9.1.4.12 endpoint index, the generated contract (request and response schemas), the desktop smoke, and the adapter-parity matrix. The Reset button now commits through the trusted in-process adapter, so the Expedition tab is handed no reducer action at all (the migration guard now requires that). Add a commit test (statistics restored, nothing else changed, unknown party). |
| 0.9.7 | 74 | 2026/09/21 | Begin the Expedition tab migration (slice E1, commands). Destination, mode, depth-limit, and difficulty-offset changes now commit through the trusted in-process `changeExpedition`, and the Sortie and Gods Battle buttons commit through the API sortie (`triggerSortie` keeps only its popups and no longer runs the reducer sequence or writes the cycle itself). Make `changeExpedition` a real validated operation: it previously accepted anything. The whole request is checked against the shared choices in the new `src/game/expeditionSettings.ts` and rejects atomically (unknown destination or depth limit and a non-even or negative difficulty offset are `invalid_request`; a locked destination, the Colosseum while disabled, and an offset above the destination's maximum, which is 0 until its boss is defeated, are `illegal_action`), and `read/expedition/{p}/setting` now publishes exactly those choices instead of a static list (all destinations, difficulty 0 to 68). The Colosseum comes from a new runtime port for the player and the API debug settings for an account. Add commit and projection tests (every offered option is accepted, atomic rejection) and a migration guard that keeps the Expedition controls off the reducer. The statistics Reset button remains a reducer action: there is no API operation for it yet. |
| 0.9.7 | 73 | 2026/09/21 | Complete the `read/observation/overview` header projection: real Gold, Prana, environment, and unread Diary count, plus the header's runtime-owned facts through a new `headerRuntime` runtime port (no refactor): `speedOfTime` (Debug-pane base speed, effective scale, and the progress-report bonus's active state and expiry, with an expired bonus not reported), `autoRepeat`, and `progressReportInfo`. An API account has no such runtime facts, so they are `null` (and the report unavailable). Replace the placeholder schema with concrete members, regenerate the 84-operation contract, and add a schema-validated test for the player runtime, an API account, and an expired bonus. |
| 0.9.7 | 72 | 2026/09/21 | Complete the Expedition projection (`read/observation/expedition`) for the pane migration: per-party `progress` (main bar, step counts, current Step sub-progress window, and `nextChangeAt`), server-gated `exploration` that publishes only the rooms revealed as of the read plus `nextRevealAt` (so no client receives a future room, HP, or outcome, and `currentHp` is the last revealed room's HP rather than the running exploration's final HP), `clearGates` and `sideQuest` facts, and `controls.sortie` and `controls.godsBattle` with stable unavailable reasons. Move the shared logic out of the UI helpers without changing behavior: goal selection and side-quest facts into `src/game/expeditionGoals.ts` (the compact progress text is now formatted from it, pinned by a frozen 409-item characterization fixture), state progress and the exploration reveal count into `src/game/partyStateProgress.ts`, and the sortie decision into `src/api/v1/sortieAvailability.ts`, which the projection and the commit share. The sortie and Gods Battle commits now also refuse an unlocked-destination sortie with `illegal_action:entry_gate_locked` (the button is disabled in that state; the Colosseum is exempt). Regenerate the 84-operation contract with the new schemas and add projection tests (reveal gating, no leaked defeat, rest/sell/continuous progress, goals, side quest, each control reason) and the refusal test. |
| 0.9.7 | 71 | 2026/09/21 | Project the real party state and enforce the no-spoiler rule in the API reads (revised Stage 4, using the runtime port added in Build 70). The Expedition and compact projections previously guessed `state` from HP (`state.rest` or `state.idle`). For the ordinary player's runtime they now report the live party cycle (`state.rest`, `state.sell`, `state.free_action`, `state.sound_sleep`, `state.pray`, `state.move`, `state.explore`, `state.return`, and so on), and the Expedition projection gains the state's clock (`stateStartedAt`, `stateDurationMs`, `stateExpectedEndAt`; `null` for idle and reactivating) so a client interpolates progress itself. While a party is in `state.explore`, `disclosedFloor`, `disclosedOutcome`, the compact `lastDestination` and `lastOutcome`, and the default `latestBattleLog` come from the log disclosed before that exploration began (a `HomeScreen` memory that mirrors the Expedition tab's own, supplied through a new `disclosedExpeditionLog` port), not from the result the engine has already resolved; a Diary-retained `logId` is unaffected. An API account has no live cycle, so it gets the Specification 5.1.1 resume state (`state.rest` below maximum HP, otherwise `state.idle`) and no clock, and no disclosure memory. The Instant Expedition `chargeStock` and `chargeDuration` now use the current Speed of Time like the UI. Regenerate the 84-operation contract and add regressions: explore with its clock, the previous log disclosed in the expedition, compact, and battle-log reads, the newest log once the party has left exploring, an idle party without a clock, and the account fallback. Step counts and sub-progress for the step-based states are not projected yet. |
| 0.9.7 | 70 | 2026/09/21 | Make `commit/expedition/{p}/sortie` and `godsBattle` behave as pressing the Sortie and Gods Battle buttons (Spec 9.1.3, 3-2-2). The API sortie was a shorter, inconsistent sequence: with no Instant Expedition charge it still ran, it ran at 0 HP, `godsBattle` ran a normal expedition when no Gods Battle was available and kept the party's side quest, the stock was consumed at the default charge speed, a running exploration's Diary entry was never finalized, sleepiness was not rolled, and the live party cycle was left where it was. It now follows the button's own reducer sequence: refuse with `illegal_action` (reasons `charge_insufficient`, `party_exhausted`, `gods_battle_unavailable`, `already_moving_to_gods_battle`; the Colosseum needs neither charge nor HP), cancel the side quest for a Gods Battle, consume one stock at the current Speed of Time, finalize the Diary entry when the party is exploring, clear the pending profit, restore HP, resolve, roll sleepiness, and reset the party to the beginning of `state.rest`. No game logic is extracted: `HomeScreen` supplies the runtime ports (`partyCycle`, `restDurationMs`, `applyPartyCycleWrites`) it already owns, and the cycle reset is returned by the commit as a write the authority applies after the durable commit and just before publication, so no tick sees the new state with the old cycle, a failed persistence resets nothing, and an API account (which has no live cycle) neither reads nor writes one. The presentation flags the button sets (reward-popup pending, Gods Battle pending) are set by the same runtime callback. The result's `logId` is now `diary:<id>` only for a Diary entry this sortie created, otherwise `latest` (it used the newest older entry before). Add regressions for every refusal and the Colosseum exception, one stock consumed, the Speed-of-Time scale, exploration finalization, the side-quest cancel, the API-account case, the persist-then-cycle-then-publish order, no write on failed persistence or refusal, and the same order through the trusted in-process adapter. |
| 0.9.7 | 69 | 2026/09/21 | Unify the runtime's outcome names with Specifications 5.1, 6.1, 7.2, and 8.3 and the API: `Return`, `Draw`, and `Retreat` replace `Turned_Back`, `Draw_Retreat`, and `Wounded_Retreat` everywhere (the Clear-Gate outcome type, the expedition statistics kept per party, the forecast counts, the Condition adjustment table, the AFK summary counts, the flavor-condition generator, and the Simulation Run), and a finished expedition is stored as `Return` instead of `Escape`. A stored `Retreat` still covers a draw and a retreat, told apart by whether the last room was a draw. Saves and runtime snapshots written before this build keep loading: hydration (`hydrateGameState`, which every load, import, and API-account path uses) renames the legacy statistic keys in place (key order is preserved) and turns a stored `Escape` in the latest log, the pending Diary log, and every retained Diary log into `Return`, and the AFK summary counts written by the runtime checkpoint are upgraded the same way (`legacyOutcomeKeys.ts`); nothing writes the old names. The API's outcome mapping is now the game's own `getCanonicalClearGateOutcome`. The pinned AFK final-state hash changes only because those names changed: the new serialization was verified byte-for-byte equal to the old one after mapping the old names to the new ones. Add regressions for the upgrade (statistics, all three log locations, key order, a current key winning) and, on the real heavy v0.9.3 save, that no legacy name survives loading. |
| 0.9.7 | 68 | 2026/09/21 | Use the outcome wording of Specification 6.1.5 and the Simulation Run everywhere the API reports an expedition outcome. `finalOutcome` of the retained battle log, the sortie and Gods Battle `outcome`, the compact observation's `lastOutcome`, and the Expedition projection's `disclosedOutcome` now return `Clear`, `Return`, `Draw`, `Retreat`, or `Defeat` instead of the stored `Clear`, `Escape`, `Retreat`, `Defeat`, and are typed as exactly those five values in the generated schema. A new `apiExpeditionOutcome` (`src/api/v1/expeditionOutcome.ts`) maps a finished log through the game's own `getCanonicalClearGateOutcome` (a stored `Retreat` is a Draw when the last room ended in a draw), so the API cannot disagree with the Clear-Gate rules. The runtime's internal names (`Turned_Back`, `Draw_Retreat`, `Wounded_Retreat`, `Escape`), which Specifications 5.1, 7.2 and 8.3 still use, are unchanged. Document the wording in 9.1.4.14 and add a regression that drives all five outcomes (including a draw and a non-draw retreat) through the compact, Expedition, and battle-log reads. |
| 0.9.7 | 67 | 2026/09/21 | Give `read/expedition/{p}/latestBattleLog` its concrete public payload (contract-fidelity closure, Stage 1). It previously returned the party's internal `lastExpeditionLog` object as is (with replay seeds and rendered legacy narration) and an always-empty `bottleneckEnemies`. It now returns `buildBattleLogData` in `battleLogs.ts`: log header, rewards as Item Format, auto-sell totals, and per room the outcome, damage, party HP, heal and attrition, end events, and the battle as compact language-neutral event rows (`compact-v1`) or the original legacy facts (`legacy-facts`), with no narration, flavor rows, or replay metadata. The optional `logId` is now honored: `latest`, or `diary:<diaryEntryId>` for the log retained by a Diary entry, otherwise `not_found`; a party with no log returns `null` and an empty list. `bottleneckEnemies` implements Spec 9.1.3 (2-2-2): rooms with at least 35% of maximum party HP in damage or a draw or defeat, each with a new public `EnemyStatus` (`enemyStatus.ts`) built from the enemy as scaled for that battle, with its effective level, raw numeric `d.`, `f.`, `c.`, `e.`, `r.` facts, abilities, bonuses, and drop item IDs; the same model will serve the Bestiary. The old Experimental-API-era `buildApiV1BattleLog` and the unused `buildApiV1DiaryEntries` (and their non-existent `Experimental AI API` SpecRef titles) are replaced. Regenerate the 84-operation contract with the concrete schema (no `Type.Unknown` in this response, event rows as typed tuples). Verified on the real heavy save (every party's latest log and every Diary-retained log validate; bottleneck counts match the rooms; enemy levels are derived) and on freshly recorded compact and legacy logs in the battle golden test. Older retained snapshots without `itemIds` report no drop candidates. |
| 0.9.7 | 66 | 2026/09/21 | Give `read/expedition/{p}/simulationRun` its concrete public payload (contract-fidelity closure, Stage 1). The response previously wrapped the internal forecast object as an unspecified `result`; it now returns the two compact strings of Spec 9.1.3 (2-2-3) and the structured numbers required by 9.1.4.9: `overview` (`Success 90.0% / Draw 4.0% / Retreat 2.0% / Defeat 4.0%`, Clear and Return not distinguished), `detail` (one string per room in `<floor>f-<room>/Success … / Not reached …` form), `overviewPercent` (success, clear, return, draw, retreat, defeat), `runs`, and 24 `rooms` with integer counts (reached, not reached, victory, clear, return, draw, retreat, defeat) and the successful and retreat remaining-HP buckets the Simulation Run graph needs, plus `simulatedRevision` and a fresh `seedDomain` per forecast. The response is built by a new `buildSimulationRunData` in `src/api/v1/simulationView.ts` (reusing the game's room coordinate helper); an absent forecast runner is `runtime_unavailable` instead of a silent `undefined`. Regenerate the 84-operation contract (no `Type.Unknown` remains in this response) and extend the read-model regression: strings and percentages against a hand-built forecast, every room accounting for every run exactly once, the published schema, and a distinct seed domain per request. |
| 0.9.7 | 65 | 2026/09/21 | Close the review points of the `equipmentEvaluation` read (Builds 62–64). `targetItems` and `equipmentChanges` are limited to 100 entries per request (published as `maxItems` in the generated schema, enforced as `invalid_request`, documented in 9.1.4.9), because the Party tab asks for one preview per owned variant of a category (about 190 on the real save, roughly 9 KB of encoded query text against a 16 KB header limit). The tab now reads in chunks with a new `useApiReadMany` hook that also waits 120 ms after the last change, so a burst of inventory changes issues one set of requests. Each previewed change computes only the target character (`computeCharacterStatsInParty`: base stats plus the party deity's per-member modifiers, equal to `computePartyStats(party).characterStats[index]`) instead of all six members; 186 armor previews on the real save fell from about 112 ms to about 39 ms. Add regressions for the limit (100 accepted, 101 rejected, schema bound), for stats equality with the whole-party computation across all ten deities, and an HTTP-shaped versus in-process parity step for the read. |
| 0.9.7 | 64 | 2026/09/21 | Complete the Party equipment defense-preview migration through the read-only `read/build/character/{characterId}/equipmentEvaluation` operation. Add optional one-or-repeated `equipmentChanges` queries alongside the existing Jewel-required `targetItems`: replacements use `<slotIndex>=<Item Format>/<jewelType>:<jewelRank>` (`0:0` means no Jewel) and removals use `<slotIndex>=0`. Each unique change is evaluated independently against one immutable snapshot and returns the exact whole-number physical- and magical-defense deltas plus aptitude, without ownership requirements, mutation, revision, receipts, equipment history, or RNG. The Party tab batches its visible category's replacements and equipped-item removals through the in-process adapter and only formats returned deltas; it no longer imports `computeCharacterStats` or `replaceCharacterEquipment`. Update both API specifications, generated schemas/examples, wire-format helpers, source guards, and parity/invalid-input/read-only tests. |
| 0.9.7 | 63 | 2026/09/21 | Move saved-equipment-set availability fully behind the shared Application API. `read/build/character/{characterId}/equipmentSet` now returns a required character-specific `availability` projection in both summary and detail modes: `allAvailable` plus ordered per-entry `slotIndex`, saved Equipment Entry, boolean availability, and stable `slot_unavailable`, `not_equippable`, or `unavailable` reasons. The calculation uses the same exact-copy, equipped-copy, aptitude, and slot rules as confirmed set loading; Jewels remain excluded because saved sets do not store them. The Party UI consumes this projection instead of importing and rerunning `evaluateEquipmentSet`, shrinking its reviewed game-logic exceptions to localized draft-name selection and the still-open slot-aware defense preview. Update the product and technical API contracts, shared TypeBox schema, generated desktop artifacts, parser/view type, migration guard, implementation plan, and focused availability/read-model/schema regressions. Correct the desktop smoke's equipment-evaluation fixture to use a Jewel type accepted by its Armor target. |
| 0.9.7 | 62 | 2026/09/21 | Correct `equipmentEvaluation` to the Read contract defined by Spec 9.1.3 (2-3-5): replace `POST /api/v1/commit/build/character/{characterId}/equipmentEvaluation` with `GET /api/v1/read/build/character/{characterId}/equipmentEvaluation`, remove the operation from the mutation authority, and accept one or repeated `targetItems` query parameters in the required `<Item Format>/<jewelType>:<jewelRank>` form. Evaluation now parses and echoes the specified Jewel and includes its scaled `d.` attack, defense, and HP values plus its `c.` attack, defense, accuracy, or evasion bonus; incompatible item-category/Jewel pairs are rejected, while ownership is not required, and the read does not change state, revision, receipts, persistence, or equipment history. Update the 9.1.4 technical contract and generated 84-operation route/schema catalogs with no compatibility alias for the obsolete Commit path. Extend focused and heavy-save regressions for the GET route, required Jewel type/rank, Jewel-inclusive parity with the Party display, strict invalid input, request order, schema validation, and read-only behavior. |
| 0.9.7 | 61 | 2026/09/21 | Implement `commit/build/character/{characterId}/equipmentEvaluation` (Spec 9.1.3, 3-3-6), the operation that calculates one item's status for one character, and bring the contract to 84 operations. `targetItems` is one `Item Format` value or a nonempty array of unique values; the response `calculatedItemStatus` returns, per item in request order, the item, whether the character has the equipment aptitude for its category (`equippable`), the item's ability IDs, and the numeric stats the Party pane prints for it: `d.` attack, attack-count, defense and HP values scaled by enhancement, Super Rare title, the item's own multipliers and the character's equipment-category bonus (for example `c.katana_x1.4`), HP also by the character's growth and vitality/mind scale, the item's `c.`, `b.`, and `e.` bonuses, and the `f.category_multiplier` and `f.item_multiplier` behind them. It is a Commit operation that never changes the game: a valid no-op with no revision change and no equipment-history entry that still records its receipt, like a `changeBuild` simulation; an unknown item, an out-of-range enhancement, a duplicate, or an empty list is `invalid_request`, and an unknown character is `not_found`. The calculation is one shared game function (`evaluateItemForCharacter`, with the character multiplier helpers now exported from `partyComputation`) and a new `parseItemFormat`. Add the row to the 9.1.4.12 index, document the operation and payload in 9.1.4.9 and 9.1.4.14, regenerate the contract, and add regressions: every value equals the Party pane's own item formula and the numbers in its printed text over more than 300 item and character combinations, the class multiplier and aptitude are character-specific, invalid requests reject atomically, and on the real heavy save every owned variant (over 5,000 evaluations) validates against the published schema without changing the state. The Party tab does not use it yet: its remaining item defense preview needs the change in the character's own defense when an item replaces another, which this operation does not report. |
| 0.9.7 | 60 | 2026/09/21 | Finish the Party tab's move off game state (Stage 5, item 4f) and add the mechanical migration check. The last three values the tab derived from game objects are now projected facts: the character's HP contribution split (`f.hp_contribution.base` and `.item`, from the shared `computeCharacterHpContribution`) and whether its own race unlock ability is active (`f.race_unlock_active`) are published in `calculatedStatus` and read back by `buildPartyStatsView`, and the party observation gains `experienceToNext` (0 at the maximum level) and `unlockedMimorianEnemyIds`; the tab no longer imports `partyComputation`, `partyLevel`, or `getUnlockedRaceAbilitiesFromBonuses`, and `HomeScreen` no longer passes `state.global.unlockedMimorianEnemyIds` to it. `buildCalculatedStatus` takes the party level. A new `tests/migratedTabs.test.cjs` fails if the Party tab imports a game module or name that is not on a reviewed list (display helpers, plus three named exceptions), references the reducer, `GameState`, or `useGameState`, or if `HomeScreen` passes it any prop that reads `state.` directly or any reducer action other than the local toast and the shared party selection; the exception list must match the tab's imports exactly, so it can only shrink. The three reviewed exceptions are recorded in the plan: the random default name chosen when a race changes (an unsaved draft), the item defense preview (needs an `equip` `simulation` parameter, an open spec question), and per-entry saved-set availability (not yet projected). Regenerate the 83-operation contract and extend the lossless regression to the HP split, the race unlock state, the experience and Mimorian facts. |
| 0.9.7 | 59 | 2026/09/21 | Add the first entry of the closed `uiPreferences` catalog (Spec 9.1.4.17) and use it for the Party inventory category (Stage 5, items 4d and 4e). `party.equipCategory.<characterId>` (one of the twelve equipment categories, default armor) is now retained per character as Spec 8.2.4 requires, instead of being a single component state that reset on every character switch and restart. Preferences are stored per save (`GameState.global.uiPreferences`, so they survive restart and backup export) and change only through `commit/setting/uiPreferences` via a new `SET_UI_PREFERENCES` reducer action; setting a value a key already has keeps the state identity (a valid no-op). The commit now validates against the catalog and rejects the whole update as `invalid_request` for an unknown key or family, a subject that is not a character in the save, a duplicate key, a wrongly typed value, or a value outside the options; the eleven untyped placeholder keys (which no screen used) are removed, and the preferences no longer live only in the in-memory API control settings, which the ordinary player never persisted. `read/observation/setting` returns the stored pairs plus a new `uiPreferenceCatalog` (family, subject, type, options, default), and the Party tab reads its retained categories from that projection and commits a change through the trusted in-process adapter. `selectedPartyIndex` stays part of the shared game state, as decided. The equipment slot list needed no change: it already renders from the party projection's equipment entries. Regenerate the 83-operation contract, and add catalog, atomicity, no-op, projection, and source-guard regressions. Verified in a browser session: the category is retained per character, falls back to the default for a character whose set lacks it, and survives a reload. |
| 0.9.7 | 58 | 2026/09/21 | Restore exact Jewel history for equipment Undo and Redo, as Spec 9.1.3 (2-3-3) requires, while saved equipment sets stay items-only (Build 57). An Undo/Redo state now records the item, slot, lock, and Jewel assignment: new `evaluateEquipmentState` and `applyEquipmentState` restore it exactly and all or nothing, so a missing Jewel, a Jewel already used elsewhere, or a Jewel not valid for its item makes the whole state unavailable (`illegal_action`, with the projection's `available: false` and reason `The undo target contains unavailable items or Jewels.`), and no other Jewel is consumed or assigned. Availability counts the copies and Jewels the character already wears, and a Jewel is counted once. The projection's `equipmentStates` entries again carry the `/<jewelType>:<jewelRank>` suffix, a Jewel-only change adds an Undo entry again, and `createEquipmentSetSnapshot` takes `includeJewels` (off for saved sets). The reducer's `RESTORE_EQUIPMENT_STATE` uses the exact path; set loading is unchanged. Spec 9.1.4.9 now separates the two contracts. Add unit tests for exact restoration, no spare-Jewel assignment, whole-state unavailability, invalid Jewels, and worn-copy counting, and extend the real-save regression to assert exact Jewel restoration on Undo, valid Jewels after a set load, and that a missing Jewel blocks Undo. |
| 0.9.7 | 57 | 2026/09/21 | Assign Jewels independently every time equipment is set (Spec 8.2.4). Saved equipment sets, Undo and Redo history entries, and set-load availability now carry and depend on items and locks only: the saved Jewel is no longer stored, reserved, restored, or required, and an older save that stored one is normalized on load. Every set load, Undo, and Redo starts with no Jewel and runs the shared, category-validated Jewel allocation, so a missing Jewel can neither make a set partial nor fail a restore, and Jewel conservation holds. A change that only moves Jewels no longer adds an Undo entry. The Super Rare title bound is no longer hard-coded in the generated request schemas (`superRareId` has no maximum and the item format accepts any non-negative title), following the updated Spec 9.1.3. Player in-process commits keep the coalescing worker autosave introduced in Build 56. Spec 9.1.4.9 is updated to the items-only equipment-set and Undo/Redo semantics. Rewrite the exact-Jewel regressions for the new contract and add cases for ignored stored Jewels, worn-Jewel detachment, incompatible Jewels, and set normalization. |
| 0.9.7 | 50 | 2026/09/21 | Fix a Build 49 regression that crashed the renderer at startup: `read/observation/party` returns its projection inside `partyInfo`, but the Home screen read the response as the projection itself, so building the party view dereferenced an undefined party and the desktop API smoke never reached its checks (`SMOKE_TIMEOUT`). The Party tab now unwraps `partyInfo`, and a source-level regression guard pins the unwrap. |
| 0.9.7 | 56 | 2026/09/21 | Fix a Party responsiveness regression found by driving the real heavy sample save in a browser: after the Party controls moved to the trusted in-process API (Build 37 onward), every equipment action waited on `commitAtomic`, which serializes and compresses the whole save synchronously on the main thread before publishing (about 1.23 s for the 6.6 MB sample save; the commit itself took 33 ms and the projection reads 2 ms), freezing the UI for over a second per tap. The ordinary player's in-process commit now queues its snapshot on the coalescing, retrying autosave (compression stays in the persistence worker) and publishes immediately, so a later write failure is handled by the existing retry and visible warning rather than by rejecting an already-visible action. API sessions are unchanged and keep the synchronous atomic path through their account store, where a failed write must fail the commit. Measured on the sample save in a dev build, Remove All and Undo on a 15-slot character with Jewels and Super Rare gear fell from about 1,200 to 1,400 ms to about 250 to 350 ms, and the longest main-thread task from about 1.2 s to 277 ms. The real-save regression run in the browser also confirmed that all 36 characters render with correct slot counts and no invalid numbers, and that Undo and Redo track every change immediately. |
| 0.9.7 | 55 | 2026/09/21 | Fix two `/api/v1` contract defects found by running the Party projections and equipment commands against a real, heavy save (`sample_savedata/Exp8,7,6,5,4,3_set_for_test_v0.9.3_dev_20260820.kemoz`: six parties, 36 characters, 2,300 item variants, 552 Super Rare variants, 34 Jewel types, all special races). First, an empty equipment slot was emitted as the number `0`, but the published schema and the specification's examples require the string `"0"`, so any character with an empty slot failed response validation (the desktop transport would have replaced the whole party read with `internal_error`); a fresh save has every slot full, so no earlier test reached it. The party and equipment projections and the command responses now emit `"0"`, `parseEquipmentEntry` takes the string form, and an older test that had encoded the numeric value was corrected. Second, the Item Format schemas capped the Super Rare title at 80 while the game defines 82 titles (Specification 1.2) and real items carry titles 81 and 82, so any equipment or inventory entry with those titles failed validation; the generated schemas for equipment entries, item stacks, `searchItems.superRareId`, and `resources/superRareList` now allow 0 to 82 (`Specification_9.1.3_API.md` still says 1–80 in two places and needs the same correction). Add a regression, skipped when the sample save is absent, that on the real save validates every party, equipment, status, search, and donation response against the published schemas; rebuilds all 36 characters and every equipment slot (items, locks, Super Rare titles, Jewels) from the projection; checks the status facts and the tab's derived view against the frozen formulas and the computed stats for all 36 characters; verifies `searchItems` returns every owned variant, every equipped item, and every Jewel with exact counts and quantities; and verifies Remove All, Undo, and exact set loading conserve every item and Jewel and restore every item, lock, and attached Jewel, and that a `changeBuild` simulation changes nothing while a confirmed change returns removed gear to the inventory. The frozen status-formula oracle moves to a shared test module. |
| 0.9.7 | 54 | 2026/09/21 | Complete the Party tab's move off `ComputedCharacterStats` (Stage 5, item 4c-3): the tab no longer receives `characterStats`. A new `buildPartyStatsView` rebuilds every character number the tab reads (base stats, slot count, defenses, evasion, accuracy potency, the three attacks and attack counts, elemental offense and resistances, and the ability list) from the projected `calculatedStatus` alone, resolving each ability's localized name and description from its id and level in the current language, and `getElementalOffenseHelpLines` now takes only the three fields it uses. `HomeScreen` no longer computes per-character stats for the tab. The tab's remaining game-logic calls are pure functions on the projected display objects: the aggregated bonus list, the HP contribution breakdown, item stat text, and the equipped-item defense preview, which recomputes one hypothetical equipment change with the shared `computeCharacterStats`. Add a lossless regression over more than 400 fixture characters (eight class pairings by ten deities) that compares the rebuilt view with the real computed stats field by field, including ability order, names, and descriptions, and extend the source guard so the tab cannot regain computed-stats inputs. Verified in a browser session: all six members render their numbers, abilities, and spell names, an ability help bubble opens, and the item defense preview matches. |
| 0.9.7 | 53 | 2026/09/21 | Specification only: update `Specification_9.1.4_API_DETAIL.md` (9.1.4.9 and 9.1.4.5) for the `changeBuild` `simulation` and `confirmation` contract: the simulation is a valid no-op that records a receipt and returns the same errors as a commit; the response members `confirmationRequired`, `warnings` (semantic keys with numeric arguments, listed), `applied`, and `current`; a missing required confirmation, an unknown value, a non-boolean `simulation`, and `confirmation` with `simulation: true` are `invalid_request`; `no` cancels without a revision change; the confirmation is not state-bound, so HTTP callers use the simulation's revision as `expectedRevision`; and the generic 9.1.4.5 challenge is scoped to operations without their own confirmation. No runtime change. |
| 0.9.7 | 53 | 2026/09/21 | Implement the updated `changeBuild` contract (Spec 9.1.3, 3-3-2) and remove the Party tab's own hypothetical-stats logic. `commit/build/character/{characterId}/changeBuild` now requires a boolean `simulation` and takes an optional `confirmation` of `yes` or `no`. With `simulation: true` it validates and reports without committing (a valid no-op: no revision change, no state change, no history entry) and returns `confirmationRequired`, `warnings`, and `applied: false`; with `simulation: false` a change that needs confirmation applies only with `confirmation: yes`, `no` cancels it without modifying the character, and a missing confirmation is an `invalid_request`. The operation owns its confirmation, so it no longer issues the generic 9.1.4.5 `confirmation_required` challenge (the catalog already listed it as not confirmation-gated). Warnings are stable semantic keys with numeric arguments (`api.warning.changeBuild.equipmentSlotReduction` with `count`, and `meleeAptitudeRemoved`, `rangedAptitudeRemoved`, `magicAptitudeRemoved` with `items`), reported only when confirmation is needed, and a new `getEquipmentAptitudeForCategory` classifies lost equipment aptitudes. A simulation reports the same validation errors a commit would, and `confirmation` combined with `simulation: true`, an unknown value, or a non-boolean `simulation` is rejected atomically. The Party editor now simulates first, shows the API's localized warnings for confirmation, and commits with `confirmation: yes` only after the player agrees; it commits directly when no confirmation is needed, and the four local functions that recomputed slot reduction and aptitude loss from a hypothetical character were removed. Regenerate the 83-operation contract (request and response schemas), update the build-change and adapter regressions (the adapter's confirmed-retry test now uses a partial equipment-set load), and add a source guard. Verified in a browser session: cancel leaves the character unchanged, confirm applies, and an unrestricted change commits without a prompt. |
| 0.9.7 | 52 | 2026/09/21 | Fix a Build 43/44 defect found by driving the Party tab in a browser: the Undo and Redo buttons reflected the equipment state from before the latest change (after Remove All, Undo stayed disabled until something else re-read the projection; after Undo, Redo stayed disabled), and a click made in that window was rejected as `illegal_action`. The equipment projection re-read on game-state changes, but the Undo and Redo history lives in the authority's control metadata, which is installed just after the state is published, so the re-read could observe the previous history. The in-process adapter now exposes `subscribe`, called after each successful commit is fully installed, and `useApiRead` re-reads its projection on that signal in addition to its own dependencies, so every projection sees the committed history. Add a regression that reads the equipment projection at the moment subscribers are notified (Undo enabled after a change, Redo enabled after an Undo, no notification for a rejected commit or after unsubscribing). Verified by driving every migrated Party control in a real browser session: Remove All, Undo and Redo, save, load, and exact-availability of equipment sets, tap to unequip and equip, character rename and class change with the aptitude-loss confirmation, deity change, drag-and-drop reordering with its confirmation, the auto-equipment mode cycle, Auto Equipment, and item lock. |
| 0.9.7 | 51 | 2026/09/21 | Continue the Party reads (Stage 5, item 4c-2): the status pane's offense, accuracy, penetration, and defense values and the status-change notifications now come from the projected `calculatedStatus` instead of formulas held inside the Party tab. The offense amplifiers (base strength or intelligence scale, deity bonus, Iaigiri, Heavy Strike, Arc Magic, equipment multipliers), the deity-inclusive defense amplifiers, the Focus-adjusted accuracy bonus and per-hit decay, and total penetration (including Heavy Strike's conversion of attack count) move into one React-free `src/game/statusFacts.ts` (`deriveStatusFacts`) that the read model and the tab share; the UI helper exports are kept as re-exports. `calculatedStatus` gains additive facts `f.offense_amplifier.melee/ranged/magical`, `f.c_accuracy`, `f.accuracy_decay`, and `f.penetration`, and `f.defense_amplifier.physical/magical` now include the deity multiplier and the 0.01 floor as Spec 2.1.1.2 defines `character.f.defense_amplifier` (they previously reported the raw pre-deity value). `readStatusFacts` reads them back, and `buildCombatTotals` builds the notification totals from the projection alone, so two consecutive notifications never compare a projected value with a local one while a refresh is in flight. The party observation now carries each member's status through `PartyView.characterStatus`. Two notification values are now unified with the status pane, as Spec 8.1.1 requires (`Same calculation and display logic of status`): the melee and ranged amplifiers include the Heavy Strike x1.4 factor, and penetration includes Heavy Strike's conversion, which the notifications previously omitted. Add a lossless round-trip regression over 400+ real characters (eight class pairings by ten deities, exercising Iaigiri, Heavy Strike, and deity offense and defense amplifiers) against a frozen copy of the old pane formulas, plus a source guard. Remaining status-pane inputs (ability list, bonus list, HP breakdown, elemental help, spell name) are recorded as 4c-3. |
| 0.9.7 | 49 | 2026/09/21 | Continue the Party reads (Stage 5, item 4c, first half): the party pane, party selector, member list, and deity assignment rules now render from `read/observation/party` instead of the `Party` game object. The party observation gains additive members: each character's `isUnique` and `mimorianEnemyId`, the party's `experience` and `maxHp`, and a `parties` list of every unlocked party's deity and member identities (used by the deity-in-use rule and the default-name and Mimorian-form rules). A new `buildPartyView` and `buildPartySummaries` rebuild the display party and members from the projection (identity facts plus equipment entries resolved against master data), the Party tab takes `PartyView` and `PartySummary` types instead of `Party`, the read refreshes only when a displayed fact changes and runs only while the tab is visible, and nothing is drawn from another party's data while a party switch loads. Add regressions that validate the projection against the published schema, rebuild every member and equipment slot losslessly, and guard the tab's props. The calculated-stats half of 4c (enriching `calculatedStatus` and moving the tab's own offense and defense derivations into shared facts) remains, as recorded in the plan. |
| 0.9.7 | 48 | 2026/09/21 | Implement `calculatedBasePower`, the new sort order, and `limit` for `read/base/searchItems` (Spec 9.1.3, 2-4-1). Every inventory stack is now `<Item Format>/<quantity>/<calculatedBasePower>` and every character-assigned item `<Item Format>/<characterId>/<jewelType>:<jewelRank>/<calculatedBasePower>`, where the power is the one `d.` stat that defines the item category (armor physical defense, robe magical defense, shield HP, sword and katana melee attack, gauntlet melee NoA, arrow and bolt ranged attack, archery ranged NoA, wand and grimoire magical attack, catalyst magical NoA) scaled by enhancement, Super Rare title, and the item's own multipliers exactly as the Inventory shows it, excluding character equipment bonuses such as `c.katana_x1.4`; an attached Jewel adds its own value for that stat before scaling. The shared item-power calculation moves from the UI helpers into `src/game/itemPower.ts` (the UI re-exports it). Equipment now sorts by higher calculated base power, then higher item id, then higher Jewel rank (further ties: higher enhancement, higher Super Rare title, stack before character-assigned, lower character id), and unassigned Jewels sort by higher rank, then higher Jewel type in specification order. The new `limit` parameter (default 10, 1 to 5000, invalid values rejected) is applied after filtering and sorting and replaces the cursor for this operation, so `nextCursor` is removed from the response; the Party tab requests the maximum 5000. The generated stack-format schema, example, and parsers are updated. Add regressions for the format, every category's base power, enhancement and Jewel effects, the sort order, the limit and its bounds (including over HTTP), and schema acceptance and rejection. |
| 0.9.7 | 47 | 2026/09/21 | Implement the refined `read/base/searchItems` contract (Spec 9.1.3, 2-4-1). `category` is optional again, so the API can search every equipment category at once (the UI still chooses a category first), and the Party tab returns to a single inventory read. The response is now only `items` (plus `nextCursor`): inventory stacks are `<Item Format>/<quantity>`, character-owned items (the `equipped` state) are `<Item Format>/<characterId>/<jewelType>:<jewelRank>` with `0:0` when no Jewel is attached, and the `jewel` category lists unassigned Jewels as `<jewelType>:<jewelRank>/<quantity>` and assigned Jewels as character-owned items; the separate `equippedItems` and `details` members are removed. The `details` parameter is implemented: `ability=[..]`, `cBonus=[..]`, and `otherBonus=[..]` are appended in that fixed order according to `none`, `ability`, `cBonus`, `otherBonus`, `abilityAndCBonus` (the default), or `all`, using a new item-details module that maps master data to ability, `c.`, `d.`, `e.`, `r.`, and `b.` IDs (multipliers print as fractions such as `c.physical-defense-x2/3`) and describes Jewels by rank; `searchAbility` and `searchBonus` filter on those same IDs. Results are ordered by item id, enhancement, Super Rare title, then stack before character-owned, then character id, with unassigned Jewels first by type and rank. The Party tab requests `details: none`. Remove the unused `useApiReads` hook, tighten the generated result-string schema, and add regressions for optional category, every state and format, each details mode, ability and bonus search, Jewel details, the HTTP contract, and schema acceptance of every documented result format. |
| 0.9.7 | 46 | 2026/09/21 | Continue the Party reads (Stage 5, item 4b): the owned inventory used for equipping and the Jewel counts now render from `read/base/searchItems` projections. Implement `searchItems` for real: it previously returned every stack and ignored its filters. It now applies `category` (`bow`, `glove`, and `book` map to the archery, gauntlet, and grimoire categories), `rarity`, `superRare`, `superRareId`, and `itemId`, honors the `owned`, `sold`, `equipped`, and `all` states, returns only owned stacks with a positive count, and orders stacks by item id, then enhancement, then Super Rare title. The `jewel` category lists Jewel stacks as `<jewelType>:<jewelRank>/<quantity>`, which gives the UI a Jewel projection. `category` stays required, as Spec 9.1.3 lists it (unlike `rarity`, it is not marked optional), so the Party tab reads the twelve equipment categories in parallel with a new `useApiReads` hook; the stack format accepts Jewel stacks. New `parseInventoryStacks` and `parseJewelStacks` rebuild the display inventory from the projection, the item rarity band moves from the UI helpers into `src/game/itemRarity.ts` so the read model can share it, and the read hooks gain an `enabled` flag so the inventory reads run only while the Party tab is visible. Pagination and the ability and bonus search remain with the Base stage. Add filter, ordering, Jewel, round-trip, and source-guard regressions. |
| 0.9.7 | 45 | 2026/09/21 | Begin the Party reads (Stage 5, item 4): the saved equipment sets and the deity pane now render from projections. Add one shared item wire-format module (`formatItem`, `formatEquipmentEntry`, `parseEquipmentEntry`, `parseSavedEquipmentSet`) so a projection carries exactly the facts needed to rebuild a display item from master data, and use it for both the read models and the UI adapter. Fix three read-model defects found while migrating: the saved-set read ignored its `isEquipmentSetDetail` parameter over HTTP (the schema coerces it to a boolean but the handler compared it to the string `'true'`, so detail was never returned) and reported the item's lock flag instead of each saved entry's own lock; and `resources/donationBox` was a placeholder that reported rank 1 and `MAX` for every god, so it now lists every unlocked god with its real rank, donated Gold, and next-rank total (`MAX` at the top rank). Add round-trip, lock-flag, detail-parameter, donation-box, and source-guard regressions. The remaining Party-read work is decomposed in `docs/api-v1-implementation-plan.md`. |
| 0.9.7 | 44 | 2026/09/21 | Align the equipment projection's Undo and Redo options with the updated Spec 9.1.3 (2-3-3). `validOptions.undoEquipment` and `validOptions.redoEquipment` are now `{equipmentStates, available, unavailableReason}`: `equipmentStates` lists every restorable state (at most 30, most recent first, the order repeated Undo or Redo restores them) as Equipment Entry strings for its equipped slots, `available` reports whether the next step can run, and the empty case returns `[]`, `false`, and the specified `No undo history.` or `No redo history.`. The other reasons use the same sentence style (`The undo target matches the current equipment.`, `The undo target contains unavailable items.`). The generated contract limits the list to 30 states, and regressions cover an empty history, a full 30-state history in order, the reasons, and that 32 real equipment changes retain exactly the 30 most recent states. |
| 0.9.7 | 43 | 2026/09/21 | Migrate equipment Undo and Redo to the `/api/v1` Application API (Stage 5, item 3). The Party tab no longer keeps its own per-character history: the Undo and Redo buttons commit `undoEquipment` and `redoEquipment` through the trusted in-process adapter and enable from the equipment projection, so one history (the API's, recorded for every equipment-changing command including build changes and set loads) drives the buttons. The equipment read model (`read/build/character/{characterId}/equipment`) now returns `validOptions.undoEquipment` and `validOptions.redoEquipment` as `{available, unavailableReason}` actions (`noHistory`, `noChange`, or `itemsUnavailable`), computed by one shared availability function that also backs the commit responses. Fix `numberOfEmptyEquipmentSlots`, which counted empty entries of the equipment array and reported 0 for a character with a short array; it now counts against the character's real slot count. Add a reusable `useApiRead` hook for projection-driven UI, remove the dead `restoreEquipmentState` reducer wrapper and the tab's history state, `recordEquipmentChange` wrappers, and `onRestoreEquipmentState` prop, regenerate the 83-operation contract, and add availability, short-array, intent, and source-guard regressions. |
| 0.9.7 | 42 | 2026/09/21 | Migrate the Party equipment-set controls to the `/api/v1` Application API (Stage 5, item 2). Save, load, rename, and delete now commit through the trusted in-process adapter via new `saveSet`, `loadSet`, `renameSet`, and `deleteSet` equipment intents: a load maps the player's explicit choice to `equipSet` (every stored item available), `equipExactMatchesOnly`, or `equipSimilar` and answers the shared partial-load confirmation round trip on their behalf, while blank or unchanged renames (the input's blur) are not committed. Fix three set-handler defects the migration would have inherited: `saveEquipmentSet` reported the last array element as the new `equipmentSetId`, which is wrong when the set fills a gap because sets are ordered by slot; saving into a full list silently did nothing instead of `illegal_action`; and renaming or deleting an unknown set silently did nothing instead of `not_found` (blank or over-long names are now `invalid_request`). Remove the dead `saveEquipmentSet`, `renameEquipmentSet`, `deleteEquipmentSet`, and `loadEquipmentSet` reducer wrappers so these controls cannot bypass the API. Add intent-mapping, handler-fix, and source-guard regressions. |
| 0.9.7 | 41 | 2026/09/21 | Implement the optional `targetSlot` parameter of `commit/build/character/{characterId}/equip` (Spec 9.1.3, 3-3-5). With `targetSlot`, exactly one item is equipped to the given slot and replaces its occupant in a single atomic commit, so the displaced item and its Jewel return to the inventory and the replacement is one revision and one Undo step; without it the items keep filling empty slots in order. `targetSlot` must be a non-negative safe integer (`invalid_request`), requires a single item (`invalid_request`), and must be within the character's real slot count (`illegal_action`). The Party equipment controls now issue that single command instead of `removeEquipment` followed by `equip`, so a replacement lands in the tapped slot. Also fix a latent `equip` defect: free slots were derived from the equipment array length, so a character with a short (for example empty) equipment array could not equip anything (`no_free_slot`); slots are now derived from the character's computed slot count. Regenerate the 83-operation contract with the new optional parameter and add regressions for replacement, validation, and short-array characters. |
| 0.9.7 | 40 | 2026/09/21 | Migrate the Party equipment controls to the `/api/v1` Application API (Stage 5, item 1). Equip, unequip, replace, lock and unlock, Jewel attach and remove, Remove All Equipment, the Auto Equipment mode cycle, and the manual Auto Equipment button now commit through the trusted in-process adapter via a new pure `planEquipmentIntent` translator, planned against the newest authoritative snapshot and serialized so a multi-command intent is never interleaved: replacing an occupied slot commits `removeEquipment` then `equip` (the API assigns empty slots in order, so a replacement can land in the first free slot rather than the tapped one), the Jewel toggle removes an attached rank or otherwise attaches, and the manual Auto Equipment popup notifications are planned from the same snapshot and shown after a successful commit. Remove the now-dead `equipItem`, `removeAllEquipment`, `toggleEquipmentLock`, `attachJewel`, and `updateCharacter` reducer wrappers and the Party tab's `onUpdateCharacter` prop so no Party control can bypass the API. Add intent-translation regressions applied to real state and a source guard. |
| 0.9.7 | 39 | 2026/09/21 | Close the remaining Stage 1 payload gaps of the `/api/v1` contract catalog. `calculatedStatus` in the Party observation and `read/build/character/{characterId}/status` is now the public `CalculatedStatus` fact model (9.1.4.14) built by a new `buildCalculatedStatus` projection of the shared computed character stats (stable `b.`/`d.`/`c.`/`e.`/`r.` keys with raw numbers and units, ability and bonus facts, and melee/ranged/magical attack facts with the shared dice profile) instead of leaking the internal computed-stats object; the ability level ceiling is raised to 10 so Lv6–Lv7 abilities such as Life Drain validate. `sellInventoryItems` and `purchaseShopItems` now return `{items: {item, quantity}[], goldDelta, pranaDelta}`: selling validates every entry against one snapshot first (duplicate variants are `invalid_request`, unknown variants `not_found`, sold, not-owned, or empty variants `illegal_action`) so a bad entry sells nothing, and purchasing reports the exact hidden enhancement and Super Rare variant drawn by the reducer through a synchronous `onPurchased` callback plus the net currency delta including auto-sell proceeds. Regenerate the 83-operation contract and add real-state regressions for the schema conformance and both result shapes. |
| 0.9.7 | 38 | 2026/09/21 | Migrate Party character editing to the `/api/v1` Application API. The editor's Done and Save actions now commit through the trusted in-process adapter to `commit/build/character/{characterId}/changeBuild` instead of dispatching `UPDATE_CHARACTER` directly: a new pure `characterEditToChangeBuildParameters` mapper sends only the changed name, combined race/gender (including the Mimorian form), class, lineage, and predisposition fields and rejects unsupported keys. The shared build-change planner remains the single validation authority; when it reports `confirmation_required` the existing equipment-loss confirmation dialog is shown, and its confirmed retry reuses the shared confirmation-token round trip. A rejected edit keeps the editor and pending selections open. Auto-equipment mode toggling stays on the reducer until the equipment-controls slice. Add mapper, applied-request, and source-guard regressions. |
| 0.9.7 | 37 | 2026/09/20 | Begin the Party UI Application API migration by making the trusted in-process adapter usable for the ordinary browser/desktop player without an API-account login while keeping unauthenticated HTTP requests rejected. Player commits now use the same serialized authority, validation, idempotency, confirmation, reducer, response, and revision handlers as HTTP; the adapter supplies revision/request/idempotency metadata, can carry an already-confirmed UI action through the shared confirmation-token round trip, persists the player snapshot atomically before publishing it, and remains separate from desktop listener enablement. Split renderer persistence from publication in the game-state port so an API-account commit no longer needs to re-persist through its renderer publication callback. Route Party deity selection and full six-member ordering through `commit/build/party/{p}` instead of direct reducer actions. Harden that operation to reject incomplete, duplicate, or foreign order sets atomically; enforce unlocked and single-Party deity selection; return a stable deity ID; project only currently selectable deity IDs; and tighten the generated request schema to exactly six unique character IDs. Add adapter tests for browser/player reads and commits without login, persistence-before-publication ordering, trusted confirmation replay, and source guards for the migrated Party commands; regenerate the complete 83-operation desktop contract. |
| 0.9.7 | 36 | 2026/09/20 | Complete the remaining transport-neutral `/api/v1` Milestone 4B character-build command behavior. Add one immutable build-change planner shared by confirmation and commit handling: it strictly validates names and stable race/gender, class, selectable lineage, and selectable predisposition IDs; enforces unique-character immutability, duplicate race/gender exclusion, female-only unlocked and unassigned Mimorian forms, and partial-update no-op semantics; and computes the same equipment-slot and aptitude invalidation warnings as the Party UI. Risky edits now create a durable `confirmation_required` reservation and apply atomically only after a valid confirmed retry, while invalid requests never receive a challenge. Character status projections now return the Mimorian target in the current stable key and advertise only currently legal race/gender and selectable lineage/predisposition options. `autoEquipment` now validates its mode and returns a concrete `autoEquipmentReport` with whether an immediate pass ran and the exact before/after item value for every changed slot. Regenerate the 83-operation desktop contract and add focused real-state regressions for validation, projection parity, confirmation, application, no-op revision behavior, and Auto Equipment reporting. |
| 0.9.7 | 35 | 2026/09/20 | Harden `/api/v1` Milestone 4B exact equipment restoration and history semantics. Persist explicit equipment slot indices in new saved sets and API Undo/Redo snapshots while migrating legacy dense sets to their historical ordering, so sparse equipment restores to its original slots. Make exact-set availability reserve exact attached Jewel key/rank counts across inventory and currently equipped items; a missing or over-requested Jewel now makes the load partial, triggers the confirmation protocol, and causes `equipExactMatchesOnly` to skip the whole unavailable entry instead of silently substituting another Jewel. Validate Undo/Redo targets before changing either history stack, reject unavailable or identical restores atomically, and record history only after a semantic equipment change so valid no-ops preserve Redo and do not advance the public revision. Complete equipment command responses with current mode, full slot/Jewel equipment, and current Undo/Redo availability; regenerate the 83-operation contract and add focused state, confirmation, history, response, and revision regressions. |
| 0.9.7 | 34 | 2026/09/20 | Continue `/api/v1` Milestone 4B with atomic `equip` and confirmed equipment-set loading. `equip` now plans the whole request against one snapshot: each Item Format is strictly validated, repeating a variant requests multiple owned copies (rejected atomically when more are requested than owned), items must be equippable by the character's aptitude (`c.equip_melee`/`ranged`/`magic`), and the request must fit the character's empty slots, so a failure applies nothing. `equip` and `removeEquipment` now demote a FULL-mode character to SEMI exactly as the UI does. Generalize the confirmation protocol (Spec 9.1.4.5) through a new state-aware `confirmationPolicy.ts`: a `loadEquipmentSet` whose exact items are all available needs no confirmation and defaults to `equipSet`, while a partial load returns `confirmation_required` with `choiceField: loadMode` and `allowedChoices` `equipSimilar`/`equipExactMatchesOnly` (never `equipSet`). The challenge reserves the base parameters, the confirmed retry may add exactly the declared choice, an unoffered or missing choice is `confirmation_invalid`, and the receipt records the complete parameters including the choice. `loadEquipmentSet` now returns a per-slot `loadReport` (`equipped`, `substituted`, or `skipped` with stable reasons `unavailable`, `not_equippable`, `slot_unavailable`), and `equipSet` is refused with `illegal_action` for a partial set. Regenerate the contract catalog for the new response shape and add mutation-verified real-state regressions for `equip` (`tests/apiV1EquipmentSlots.test.cjs`) and the partial-load challenge/confirm flow (`tests/apiV1EquipmentSetLoad.test.cjs`). |
| 0.9.7 | 33 | 2026/09/20 | Begin Milestone 4B of the `/api/v1` plan by extracting slot-addressed equipment commands into a pure planner (`src/api/v1/equipmentSlots.ts`) that validates each `removeEquipment`, `lockEquipment`, `unlockEquipment`, `jewelAttach`, and `jewelRemove` request atomically against one snapshot before any reducer action runs. Fix `jewelRemove`, which previously dispatched a placeholder jewel and silently did nothing; it now removes each attached jewel and returns it to the Jewel inventory. Reject malformed or duplicate slot indices, out-of-range slots, and malformed jewel formats with `invalid_request`, and empty slots, missing attached jewels, incompatible or unowned jewels, and lock operations outside FULL auto-equipment mode with `illegal_action`; treat re-attaching the identical jewel and locking an already-locked slot as valid no-ops instead of the reducer's silent toggle. Add an isolated real-state regression test (`tests/apiV1EquipmentSlots.test.cjs`) that was mutation-verified to fail against the old placeholder behavior. |
| 0.9.7 | 32 | 2026/09/20 | Complete the `/api/v1` Milestone 2 extraction by moving the entire Application API dispatcher out of `HomeScreen.tsx` into the transport-neutral `src/api/v1/applicationApi.ts`. `createApplicationApi(ports, state)` now owns operation routing for Fundamental, Help, popup-stream, Read/Resource, and Commit operations, serializes the account/session lifecycle, derives control metadata and the logical clock from the authority's single committed snapshot, and keeps the player's live state aligned while no API session is active. `HomeScreen.tsx` only supplies trusted runtime ports (account store, player-save export/flush/return payload, import, RNG, clock, auto-equipment, forecast simulation, renderer publication) and a session-active callback, and no longer references the dispatcher, authority, read models, session lifecycle, or commit dispatch; the now-dead revision, lease, and control refs were removed. Add the trusted in-process adapter (`createInProcessAdapter`) that supplies revision and idempotency metadata on the caller's behalf, so React and Desktop callers use the same handlers as HTTP. Add a parity fixture that runs one canonical 16-step matrix (reads, no-op and mutating commits, illegal action, unknown operation, invalid path parameter) through an HTTP-shaped request and through the in-process adapter against identical deterministic ports and asserts identical data, revisions, effects, changed resources, durable receipts, persisted revision, and publication counts, plus a source-level guard that the dispatcher does not return to `HomeScreen.tsx`. The desktop listener stays test-only. |
| 0.9.7 | 31 | 2026/09/20 | Implement the specified external-delivery job lifecycle (Spec 9.1.4.15) as transport-neutral modules `src/api/v1/deliveries.ts` and `deliveryCompletion.ts`, closing the gap where `progressReport` and `feedback` only ever created a `queued` record. The state machine serializes attempts and claims the oldest queued job; a confirmed pre-send failure is retried up to three attempts, a definitive rejection fails, and an ambiguous outcome becomes `unknown` and is never resent automatically; a process restart while `sending` (recovered on API login) becomes `unknown`. `commit/setting/backup/import` and `backup/reset` are now refused with `illegal_action` while a send is in flight and otherwise cancel every queued job so it can never send for or reward the replacement save, while terminal and unknown jobs are retained. A delivered job completes exactly once through a serialized completion transaction that reuses the existing feedback cooldown and `GRANT_FEEDBACK_REWARD` reducer (10 Prana when eligible, cooldown restarted on every success, no reward for queued, failed, unknown, or cancelled jobs); the server-owned cooldown is stored in non-exported API control metadata. `read/setting/delivery/{deliveryId}` now returns exactly the six specified members (`deliveryId`, `status`, `createdAt`, `updatedAt`, `failureReason`, `rewardApplied`) instead of the stored record, so feedback text and parameters are never disclosed, and the generated contract catalog was tightened to match. Also make account-store temporary-file cleanup best-effort so a cleanup failure can no longer mask a write or rename outcome, and remove an unused import, leaving `npm run lint` fully clean. The network sender port and immutable attachment-byte retention are not wired yet, so jobs remain queued in the running desktop until that follow-up; the listener stays test-only. |
| 0.9.7 | 30 | 2026/09/20 | Continue `/api/v1` Milestone 2 by extracting the `fundamental/signUp`, `logIn`, and `logOut` account/session lifecycle out of `HomeScreen.tsx` into the transport-neutral `src/api/v1/sessionLifecycle.ts`. The module owns userId validation, fresh-save account creation, login's ordered catch-up staging (flush the player's own save, stage its return payload, run private multi-Chunk elapsed progression when at least 60s have elapsed, durably commit the account, only then swap the live runtime state), and logout's durable account persistence followed by exact player-save restoration and return-payload consumption, all through injected ports (account store, player-save flush/export, return-payload storage, `importGameState`, clock, and catch-up collaborators) instead of closing over React refs. `HomeScreen.tsx` now only supplies those ports and applies the returned session/outcome. Added an isolated esbuild-bundle-and-spawn behavioral test (`tests/apiV1SessionLifecycle.test.cjs` / `tests/support/apiV1SessionLifecycle.profile.ts`) covering invalid-userId rejection, successful sign-up, login's `control_unavailable` against an already-active session, login's `not_found` for an unknown account (with no return payload staged beforehand), a below-threshold login that still durably commits the advanced in-game clock without running catch-up, logout's exact restoration and one-time consumption of the staged return payload, `login_required` for a session-less logout, and a safe `save_failed` when no return payload was staged. Keep the desktop listener test-only pending the remaining projections, delivery, popup grouping, and UI migration work. |
| 0.9.7 | 29 | 2026/09/20 | Continue `/api/v1` Milestone 2 with a long-lived serialized Application API authority and atomic elapsed progression. The authority now owns the committed immutable state/control/clock snapshot, serializes distinct writes, rejects a matching admitted duplicate with `operation_in_progress` plus `Retry-After: 1` (or changed parameters with `idempotency_conflict`), and lets reads continue against the prior committed snapshot while work is staged. `commit/progress/elapsed` now resolves its target once, applies AFK efficiency and Speed-of-Time cycle scaling, processes per-party full or terminal partial 30-Cycle Chunks through the shared AFK worker reducer path in stable scheduler-priority order under the serialized transaction, recalculates each Party status at its next Chunk boundary, applies automatic equipment at that boundary, yields responsively without releasing authority, and persists/publishes only the final result with one public revision. Gameplay draws use a transaction-local RNG high-water state stored in non-exportable API control metadata, so failed Chunks neither consume the renderer RNG nor retain staged RNG, state, clock, logs, receipts, or revision. API-account login catch-up now uses the same private Chunk staging boundary. Added authority fixtures for admitted duplicates/conflicts, read isolation, multi-Chunk single-persist/single-publish success, intermediate-Chunk rollback, RNG isolation, clock-only revision behavior, real-time capping, and future-clock no-ops; the desktop smoke now exercises elapsed progression. Keep the desktop listener test-only pending the remaining projections, delivery, popup grouping, and UI migration work. |
| 0.9.7 | 28 | 2026/09/20 | Continue `/api/v1` Milestone 2 by extracting commit admission and transaction ownership from React into the transport-neutral `src/api/v1/authority.ts`: the shared authority now owns canonical request identity, receipt-first replay and conflicts, tombstones, stale-revision rejection, durable confirmation reservations, immutable state and control staging, exact no-op revision behavior, changed-resource projection, popup buffering, receipt eviction, manifest-before-publication ordering, and stable commit-error classification. `HomeScreen.tsx` now supplies trusted gameplay, persistence, and publication adapters only; desktop control metadata uses the authority's typed receipt and confirmation records. Added isolated authority fixtures covering mutation success, input immutability, lost-response replay, receipt-before-revision ordering, stale rejection, injected persistence rollback with no publication, revision-neutral durable no-op receipts, post-persistence renderer loss as durable success, and stable confirmation retries. Keep the desktop listener test-only pending complete contract conformance and UI migration. |
| 0.9.7 | 27 | 2026/09/20 | Begin Milestone 2 of the `/api/v1` plan by extracting the game-mutation dispatch out of `HomeScreen.tsx` into a standalone, transport-neutral module (`src/api/v1/commitOperations.ts`): `applyApiV1Commit` now handles every `commit/*` operation (backup export/import/reset, progress elapsed/progressReport, expedition changeExpedition/sortie/godsBattle, party build, character build and equipment including undo/redo history, base jewel-priority/sell/purchase/unlock/markSeen, diary settings/markAsRead, and setting markNewsAsRead/clairvoyanceReset/modeSelect/enemyEditPane/debug/uiPreferences) through explicit injected dependencies (RNG/clock/settings-bag/history-bag) instead of closing over React refs, with HomeScreen now only wiring context and applying the returned outcome. Added an isolated esbuild-bundle-and-spawn behavioral test (`tests/apiV1CommitOperations.test.cjs` / `tests/support/apiV1CommitOperations.profile.ts`) covering success/no-op state-identity, not_found/illegal_action/invalid_request error classification, debug-mode gating, backup/reset control-event signaling, delivery-record creation, elapsed-time bounds, and the full equipment-history save/undo round trip. Idempotency/receipt/revision bookkeeping and sortie/godsBattle popup-event emission remain intentionally unextracted for a later `src/api/v1/authority.ts` milestone. |
| 0.9.7 | 26 | 2026/09/20 | Close the remaining `/api/v1` Milestone 1 contract-catalog gaps: add per-operation applicable error-code lists (derived from access level, transport, path templates, and confirmed handler behavior) and environment/unlock restriction metadata (debug-mode gating, login requirement), generate concrete full response-envelope schemas (not just the `data` payload) for both the read and commit envelope shapes and validate every outgoing envelope at runtime, add `nextCursor` to every paginated list response, and add the spec-normative shared TypeBox contract types (`NumericFact`, `AbilityFact`, `BonusFact`, `CalculatedStatus`, `EquipmentSet`, `DiaryEntry`) to `src/api/v1/contracts.ts` with a cross-check test guarding them against drift from the generated JSON catalog. Fixed a latent inconsistency in `contracts.ts` (`IsoTimestampSchema`/`SemanticTextSchema` used the unsupported ajv `format` keyword and an unbounded key length) uncovered while adding that cross-check. |
| 0.9.7 | 25 | 2026/09/20 | Generate concrete per-operation `/api/v1` response schemas (data-shape only) for all 83 operations, plus a stable normative error catalog, and compile them into runtime Ajv validators; the desktop transport now validates every produced response before it is sent and fails closed with `internal_error` on any drift instead of returning a malformed payload. Fixed two response/implementation mismatches this uncovered: internal game-mode fields use the `mode.normal`/`mode.orca` convention rather than the identity-level `normal`/`orca` enum, and the persisted API-account identity normalizes `levelOffsetForOrca` to `null` (not omitted) for non-Orca accounts. |
| 0.9.7 | 24 | 2026/09/20 | Generate strict per-operation path, query, and body schemas plus request examples for all 83 `/api/v1` routes; make the test-only desktop transport validate directly from those deterministic TypeBox/Ajv artifacts, including required parameters, types, enums, ranges, defaults, duplicate arrays, pagination limits, and corrected query names. |
| 0.9.7 | 23 | 2026/09/20 | Add the test-only staged `/api/v1` Application API foundation while keeping ordinary packaged listeners disabled until full conformance: generate and verify the 83-operation contract catalog, validate requests with TypeBox/Ajv artifacts, add serialized revision/idempotency transactions and generation-based manifest-last API-user storage, securely create per-launch loopback descriptors in smoke tests, switch and restore desktop saves on login/logout, add shared projections and commits including isolated 100/1,000-run simulations, multipart backup/feedback admission, and popup SSE replay; remove the obsolete Experimental API, AI Play evaluation, client, report-generation, and training runtime. |
| 0.9.7 | 22 | 2026/09/20 | Specification only: define private multi-Chunk API progression staging, internal versus public revisions, exclusive mutation authority, atomic final persistence, rollback/crash recovery, and required contract checks. No runtime change. |
| 0.9.7 | 22 | 2026/09/20 | Specification only: clarify API retry and confirmation ordering, multipart requests, external delivery status and reward completion, payload conventions, persisted UI ownership, work/stream lifecycle, and intentional fresh 100-run compact observations; align sold-item unlocking references. No runtime change. |
| 0.9.7 | 22 | 2026/09/19 | Document canonical Korean terminology for exploration, exploration locations, expeditions, and sorties so their distinct UI meanings remain preserved. |
| 0.9.7 | 21 | 2026/09/19 | Correct Korean desktop Draw notifications to use the distinct `파티 무승부` label, normalize the named Gods Battle compound to `신마{god}전`, and re-indent the Korean dictionary for maintenance. |
| 0.9.7 | 20 | 2026/09/18 | Persist deity identity and donation keys as stable identifiers, migrate every legacy localized deity name including Korean, and localize names only at display time so language changes retain deity selection, effects, and rank. |
| 0.9.7 | 19 | 2026/09/18 | Add Korean (`ko`) as a runtime language with lazy-loaded localization, Korean browser-language detection, persisted and URL language selection, desktop snapshot support, a separate locale bundle, and full dictionary regression coverage. |
| 0.9.7 | 18 | 2026/09/16 | Update UI_DIARY runtime (Spec 8.5): retain a maximum of 12 entries per Party Diary and display `49+` on the main unread badge when 49 or more entries are unread. |
| 0.9.7 | 17 | 2026/09/16 | Keep inline character chibis visible after language changes by matching the current party name to the retained actor's historical appearance and stable character ID. |
| 0.9.7 | 16 | 2026/09/16 | Render retained compact battle-log character names from the current party member identified by stable character ID, so localized default names and later name changes match the active language while historical battle appearance data remains intact. |
| 0.9.7 | 15 | 2026/09/15 | Store new Diary and expedition history as versioned compact semantic events with pooled historical actors and item variants; render full narration and metadata in the selected language on demand, preserve legacy text and save durability, and expose narration-free retained battle facts through Experimental API schema 2. |
| 0.9.7 | 14 | 2026/09/13 | Correct API sortie and latest-expedition outcome classification so successful depth-limit and Clear-Gate returns count as Turned_Back; derive outcomes from the shared return reason while preserving historical statistics, reports, and stored replay responses. |
| 0.9.7 | 13 | 2026/09/12 | Add optional compact simulation summaries, room outcome tables, and exact UI HP buckets to the Experimental AI API; support candidate changes-only projection, preserve legacy responses, and default the reference client to compact room results. |
| 0.9.7 | 12 | 2026/09/12 | Add the `B` X-axis label at Room 24 of the Simulation Run graph while retaining the floor-start labels at Rooms 1, 5, 9, 13, 17, and 21. |
| 0.9.7 | 11 | 2026/09/12 | Update the Simulation Run graph to label the six floor starts at Rooms 1, 5, 9, 13, 17, and 21; remove visible Y-axis percentage labels; add 25%, 50%, and 75% dashed guides; and show localized floor-room coordinates with the global room number in each reachable-room tooltip. Preserve 1,000 isolated forecasts, all 24 100% stacked room bars, outcome aggregation, colors, and touch/keyboard interaction. |
| 0.9.7 | 10 | 2026/09/12 | Update Simulation Run room tooltips to show exactly one successful outcome label: Victory for non-terminal rooms, Clear at expedition completion, or Return at the configured return-depth terminal room. Preserve all outcome counts, stacks, colors, and localized Draw, Retreat, Defeat, and Not reached values. |
| 0.9.7 | 9 | 2026/09/12 | Correct the Stealth normal-melee check in the production combined COMBAT path; retain the restriction to normal melee attacks, the HP threshold, and Pursuit bypass while allowing all reactive attacks to deal damage. |
| 0.9.7 | 8 | 2026/09/12 | Restrict Stealth to nullifying only normal melee attacks while the target is at or below its level threshold; retain Pursuit as its bypass, permit ranged, magical, and reactive damage, preserve enemy Stealth behavior, regenerate the native WASM battle runtime, and update localized descriptions and melee-specific battle logs. |
| 0.9.7 | 7 | 2026/09/12 | Complete the UI_EXPEDITION Simulation Run graph with exact specified successful and retreat HP color ranges, a lightened equal Sub/Accent Draw color, and exact Sub/Accent terminal colors; retain 1,000 isolated asynchronous forecasts and 100% room stacks. Restrict result-only battle output to discarded private forecasts so AFK, online, Gods Battle, API, expedition, and Diary records retain full narration. |
| 0.9.7 | 6 | 2026/09/12 | Split successful room outcomes in the expedition simulation graph into eight remaining-HP ranges, using the specified progressively lighter Sub colors while preserving semantic outcome totals and 100% room stacks. |
| 0.9.7 | 5 | 2026/09/12 | Replace the expedition forecast's single aggregate result bar with an accessible 24-room 100% stacked outcome graph, including reached and not-reached counts, localized room tooltips, and retained overall Clear or Return summaries. |
| 0.9.7 | 4 | 2026/09/11 | Fix exact equipment-set and Undo/Redo restoration to reserve and reattach each saved compatible Jewel key and rank before automatic Jewel assignment; retain automatic fallback only when the saved Jewel is unavailable and retain generic Jewel selection for Similar loads. |
| 0.9.7 | 3 | 2026/09/11 | Extend equipment Undo and Redo to independent per-character 30-state LIFO histories; each new equipment change clears Redo, and every restore remains guarded by the shared exact saved-set availability validation. |
| 0.9.7 | 2 | 2026/09/11 | Add equipment Undo and Redo controls that restore the immediately preceding or undone exact equipment state only when the shared saved-set availability, aptitude, duplicate-item, and slot validation succeeds; retain disabled visible controls for unavailable restores and protect the reducer against stale restore requests. |
| 0.9.7 | 1 | 2026/09/11 | Update the runtime and package release metadata to v0.9.7 and reset the release build number to 1. |
| 0.9.6 | 22 | 2026/09/11 | Fix FULL Auto Equipment popup notifications to render the final equipped item after Jewel assignment, including the localized attached-Jewel suffix such as `[堅6]`; retain replacement and party/character notification wording, notification settings, and auto-equipment behavior. |
| 0.9.6 | 21 | 2026/09/11 | Increase every AFK emulation-efficiency time band and the catch-up cap fivefold: resolve up to 810 elapsed AFK hours into up to 225 effective hours using the specified 45–810h bands; preserve the speed multipliers, persisted-backlog limits, recovery scheduling, and refresh behavior. Update the four localized runtime glossary entries, player mechanics guide, AFK profiling workloads, and regression coverage. |
| 0.9.6 | 20 | 2026/09/10 | Add current-lineup Experimental API shop observation and revision-safe purchases with authoritative hidden enhancement and Super Rare draws; expose owned equipment variants; extend atomic party configuration with ordered base-item equipment replacement, highest-enhancement-first shared allocation, explicit final modes, and targeted post-assignment Auto Equipment; update the reference client, operator guidance, recommended opening workflow, and regression coverage. |
| 0.9.6 | 19 | 2026/09/10 | Rewrite the recommended opening build as an executable Experimental API workflow using legal configuration payloads, current-observation validation, targeted resource concentration, simulation and diagnostic-sortie gates; add revision-guarded reference-client shortcuts for character-level Remove All Equipment and Auto Equipment while retaining the prohibition on shop purchases and direct slot selection. |
| 0.9.6 | 18 | 2026/09/09 | Add the character-level Experimental API `remove_all_equipment` command using the authoritative UI operation, returning all equipment and attached Jewels to shared inventory, preserving manual-removal lock and Super Rare behavior, applying FULL-to-SEMI mode and HP synchronization rules, exposing legal actions and committed effects, and updating API training artifacts. |
| 0.9.6 | 17 | 2026/09/09 | Harden the AI Play reference client with file-based configuration input, immediate shutdown renewal stops, queued-action cancellation, responsive quit and signal handling, explicit input-handle cleanup, confirmed or failed release reporting, and request-progress journals for uncertain simulations and mutations. Preserve server leases, evaluation state and scoring. |
| 0.9.6 | 16 | 2026/09/09 | Add public before/after party comparisons to API simulations and previews, structured configuration field violations across party members, and reference-client display of evaluated combat values and comparisons. Preserve candidate evaluation, atomic validation, forecast count and scoring. |
| 0.9.6 | 15 | 2026/09/09 | Add an API-only AI Play reference client with file-based configuration input, serialized requests, lease maintenance, revision tracking, durable explicit mutation retry, terminal guards, compact summaries and sanitized response artifacts. Document opening party rebuilds, client operation and recovery. Preserve API contracts, equipment selection and scoring. |
| 0.9.6 | 14 | 2026/09/08 | Refine AI Play regulation v2 with mode-bound Normal/Orca sessions and immutable rules identity, private launcher handoff and readiness checks, compact accounting with separate ledger and terminal reports, final status/configuration tables and winning-batch evidence, explicit return reasons, and evaluation-effective availability and race validation. Preserve equipment selection and exact-batch scoring. |
| 0.9.6 | 13 | 2026/09/08 | Increase the AI Play evaluation budget to 20,000 counted API calls across transaction accounting, remaining-call summaries, failure handling, and checkpoint recovery. Preserve success on the final allowed call and reject gameplay after termination. |
| 0.9.6 | 12 | 2026/09/07 | Add isolated Desktop Orca AI Play sessions with durable 200-call evaluation accounting, atomic API commits and idempotent mutations, Simulation Run and party-preview endpoints, validated whole-party configuration, a public mechanics catalog, lease renewal, boss-clear observations and automatic evaluation reports. Resolve API sorties through complete party-local Cycles with actual reward, gate and side-quest summaries. |
| 0.9.6 | 11 | 2026/09/06 | Avoid repeated inventory availability scans for unchanged records during AFK profit processing; evaluate committed Chunk availability from touched inventory and Jewel keys before automatic equipment, normalize FULL stamps after equipment commits, and defer planner inventory copies while reusing character slot counts. Preserve Chunk boundaries, FIFO authority, equipment selection rules, and save formats. |
| 0.9.6 | 10 | 2026/09/06 | Update FULL automatic Jewel allocation to treat Inventory Jewels and jewels already equipped by the character as one candidate pool, preserve the best compatible equipped Jewel when it remains optimal, detach replaced Jewels before committing reassigned Jewels, and prevent repeated Auto Equip runs from alternating between lower- and higher-rank allocations. |
| 0.9.6 | 9 | 2026/09/06 | Replace automatic FULL equipment rebuilding with dirty-check-driven, slot-order simulation that preserves locked and Super Rare equipment, upgrades only eligible candidates, persists global availability revisions and per-party FULL stamps, limits Jewel allocation to the Jewel Priority Party during FULL processing, and supports forced Party/API FULL runs while retaining scheduled no-op skips. |
| 0.9.6 | 8 | 2026/09/05 | Implement equipment management with Remove All Equipment, 99 persistent editable saved-set slots, exact availability and equipment-aptitude checks, exact or similar loading, lower-enhancement substitution with Super Rare exclusion, independent auto-equipment Jewel assignment, restored item locks, localized controls, unavailable-item styling, save migration, and focused regression coverage. |
| 0.9.6 | 7 | 2026/09/05 | Implement expanded passive, reactive, and START ability tiers through Lv5 (Stealth through Lv3), including stat calculations, native battle effects, ability upgrades, and displayed level scales. |
| 0.9.6 | 6 | 2026/09/05 | Update Speed of Time progress-report Discord usernames to the specification-defined `KEMO dev`, `KEMO beta`, `KEMO orca`, and `KEMO prod` values for their respective environments. |
| 0.9.6 | 5 | 2026/09/05 | Add bounded profile-only AFK automatic-equipment phase totals, default the isolated live profiler to the production coordinator-authority path, support the Orca environment, and report document-load and startup-to-result timings plus clearer Electron launch failures; diagnose the sandboxed macOS registration abort and validate equipment notification suppression and streamed application against exact action-prefix and input-isolation oracles; reject and remove both candidates after the five-sample 162-hour screen missed the 8% recovery improvement threshold, with streaming also exceeding the 50 ms transaction limit; retain existing production equipment, FIFO, worker, save, and gameplay behavior. |
| 0.9.6 | 4 | 2026/09/05 | Fix the `/orca/` Enemy Level Offset at +5 across initialization, persisted-state projection, Settings, live expeditions, AFK processing, simulations, and Experimental API actions so no saved or in-app value can change Orca enemy levels. |
| 0.9.6 | 3 | 2026/09/05 | Make `mode.orca` fixed in the `/orca/` environment by disabling both mode choices in Settings and rejecting any in-app runtime-mode change before it can alter the enforced Orca state. |
| 0.9.6 | 2 | 2026/09/05 | Restore the localized Community link alongside the separate Orca link in Developer News: show the Orca link as the localized application title followed by `orca`, route Simplified Chinese to Telegram, and route all other languages to the localized Discord community label. |
| 0.9.6 | 1 | 2026/09/05 | Apply the effective Speed of Time multiplier to Instant Expedition Charge accumulation and display, including the rounded-up x5 sixth-slot duration of 39 minutes; retain compact MAX charge indicators; update the all-language Developer News community link to the Orca page; and verify environment-scoped persistence for runtime mode and Orca enemy-level offset. |
