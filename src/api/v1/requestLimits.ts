// SpecRef: 9.1.4.14 | Parameter and payload schema conventions | Request limits
/** The most `targetItems` or `equipmentChanges` one `equipmentEvaluation` request may carry (keeps a GET query within header limits). */
export const EQUIPMENT_EVALUATION_LIMIT = 100;
