name: Deploy Pages (luna environment)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Build luna artifact
        run: |
          set -euo pipefail

          if git ls-remote --exit-code --heads origin luna >/dev/null 2>&1; then
            git fetch origin luna
            git worktree add ../kemo-luna origin/luna
          else
            echo 'Remote luna branch not found; using main build for /luna/ fallback.'
            git fetch origin main
            git worktree add ../kemo-luna origin/main
          fi

          npm ci --prefix ../kemo-luna
          npm run build --prefix ../kemo-luna

          rm -rf site
          mkdir -p site/luna

          cp -R ../kemo-luna/dist/. site/luna/

          cat > site/index.html <<'HTML'
          <!doctype html>
          <html lang="ja">
            <head>
              <meta charset="UTF-8" />
              <meta http-equiv="refresh" content="0; url=./luna/" />
              <meta name="viewport" content="width=device-width, initial-scale=1.0" />
              <title>Kemo Expedition</title>
            </head>
            <body>
              <p>Redirecting to <a href="./luna/">αテスト(luna)</a>...</p>
            </body>
          </html>
          HTML

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './site'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
